# 孤儿服务判别表（W3-3 · 2026-09-03 处理记录）

> 判别标准：契约 Provide 进 DI 后，全仓（packages + host，不含备份/文档）**零消费方**
> （无 `Get<T>`、无构造注入、无静态桥接取用）= 孤儿。逐个给出处置与依据。

| # | 服务 | 定义/注册 | 消费方 | 处置 | 依据 |
|---|---|---|---|---|---|
| 1 | `IStatusPoller` | shell-status/StatusPlugin L54 Provide | **0**（面板由 `_poller` 字段直接驱动） | ✅ **删 Provide**（用户已确认） | 接口成为"对外承诺但无人取"的准孤儿；保留契约与实现，只摘无消费方的注册 |
| 2 | `IWindowThumbnailService` | shell-window-tracker（契约+实现+Provide） | **0**（Dock 缩略图走自有 ThumbnailWindow/DwmThumbnail） | ✅ **删契约+实现+Provide** | OPEN_SHELL_INTEGRATION_STEPS.md "待办：DockWindow 改为 inject" 的旧 TODO 已被 Dock 自有实现覆盖；薄封装无独立价值；`DwmThumbnailInterop`（有 DwmThumbnail 消费）保留 |
| 3 | `IDesktopSurface` | shell-core/ShellCorePlugin Provide | 0 个代码消费方 | **保留** | 多份设计文档（menu-bar/context-menu/dock DESIGN.md）明示"窗口几何统一走 IDesktopSurface"是目标架构态；删除会截断演进路径 |
| 4 | `IPowerManagement` | kernel PowerManagement 自 Provide | 仅 kernel-tests | **保留** | kernel 级基础能力（M10 冻结面），测试覆盖，成本为零；start-menu 电源菜单文档明确它是既定数据源（接口名演进中） |
| 5 | `INotificationService` | shell-notification Provide | 0 个外部消费方（插件内部自用） | **保留** | README 对外契约承诺（"消费方通过 Inject 声明依赖"）；近未来接入面，注册无成本 |

## 附带发现（同轮清理）
- `shell-context-menu-tests` 有 15 个真实用例却**从未入 sln**——`dotnet test` 全量跑从未执行过它。
  已随本轮接入 slnx（+MenuIconCacheTests 后 24 用例）。
- 空壳提案包 4 个（Companion/DynamicDesktop/FrostedShell/ThemeCenter）→ `git mv` 至
  `docs/design-proposals/`（packages 目录约定 = csproj+README，它们不满足）。
