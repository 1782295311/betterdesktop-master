# 实现拆解记录（索引）

> 日期：2026-08-22　基线：shell-app-source / shell-dock 设计稿落实复核完成，构建 0 警告 0 错误。
> 每个任务一份独立文档（`docs/实现拆解/`），本文是索引：基线、依赖顺序、风险。

## 现状基线（已落实，勿重复做）

- shell-app-source：独立 `AppSourcePlugin`（Provide IAppSourceService + IAppIconService）；图标/解析器已上移；csproj 已去 shell-core。
- shell-dock：`Inject` 消费 + `context.Get<>()`；`IDockPinnedService` 已复活并 Provide；`DockService` 主键统一 DockItemId；`DockIconService(IAppIconService)`；测试 FakeAppSource mock 重写。
- 验证命令（基线）：`dotnet build packages/shell/shell-dock-tests/... -p:Platform=x64 -p:UseSharedCompilation=false -nodeReuse:false -m:1` → 0/0。

## 任务清单

| # | 优先级 | 任务 | 文档 |
|---|--------|------|------|
| 1 | P0 | 拆 `shell-app-center`（AppGrabber/Launchpad/新装通知独立插件） | [任务01-shell-app-center拆分.md](实现拆解/任务01-shell-app-center拆分.md) |
| 2 | P1 | 新建 `shell-status` 纯采集包（8 monitor，温度后置） | [任务02-shell-status采集包.md](实现拆解/任务02-shell-status采集包.md) |
| 3 | P1 | `shell-menu-bar` 骨架 + 首期右区组件 | [任务03-shell-menu-bar骨架.md](实现拆解/任务03-shell-menu-bar骨架.md) |
| 4 | P1 | `shell-context-menu` 实现（MenuService/FileClassifier/三档） | [任务04-shell-context-menu实现.md](实现拆解/任务04-shell-context-menu实现.md) |
| 5 | P2 | 控制中心 + 台前调度简化版 + 温度/通知 | [任务05-控制中心与台前调度.md](实现拆解/任务05-控制中心与台前调度.md) |

## 依赖顺序

```
任务 1（app-center 拆分）───┐
任务 2（shell-status 采集）─┼──→ 任务 3（menu-bar 骨架）──→ 任务 4（context-menu）──→ 任务 5（控制中心/台前调度/温度）
                            └──→ 任务 4 的 Dock 项/状态组件场景
```

- 任务 1 独立可先做；任务 2 与 1 并行；任务 3 依赖 1+2；任务 4 依赖 3；任务 5 依赖 2+3。
- 每任务完成即跑构建 + 手动冒烟，不堆积（小步快跑铁律）。

## 风险与注意

- 任务 1 拆分时注意 `DockPlugin` 里 `ShowAppGrabber` 的既有调用点全部迁移，别留死引用。
- 任务 4 的 COM（IContextMenu2/3 消息泵）是"不能随便设计"的高风险点，先做 native 档再上 unified。
- 任务 5 温度依赖第三方库，先确认 LibreHardwareMonitor 与 net8 兼容；天气需和风 key（未列入本记录，单独确认）。
