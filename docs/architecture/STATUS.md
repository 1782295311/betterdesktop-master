# STATUS.md — 当前阶段与交接指针

> 地位：**新会话接手的第一入口**。动手前必须先读本文件。
> 更新义务：每阶段结束、每次重大决策落地后更新（**并同步"最后核实"日期**）。
> **最后核实：2026-09-20。**
>
> **本文件的边界**：只回答"**现在什么状态、该做什么**"。
> 实测数字、谁在跑、部署流程、操作禁忌、"完成"的四级定义 —— 全在
> [`2026-09-20-runtime-facts.md`](2026-09-20-runtime-facts.md)。
>
> 【为什么拆开】本文件原先把"入口"与"实测快照"写在一起，涨到 2009 词（预算 500）——
> **入口被细节挤掉了**。入口必须短（30 秒读完），细节必须长（留得住现场），两者放一起会互相伤害。

---

## 0. 三十秒版

| 问题 | 答案 |
|---|---|
| 架构是什么 | **Rust `core` 唯一常驻**，其余按需拉起（换掉了"8 进程常驻 + 网状互拉"） |
| 源码到什么程度 | **S1–S5 源码全部完成**（core 单测 191/191、`architecture-guard` PASS、clippy 0） |
| 为什么感觉"做了很多却没进展" | **真机验证被 B1 卡住**：8 个发布组件里只有 **3 个**能从干净检出构建 |
| 此刻机器上在跑什么 | **5 个进程**：core（**09-20 12:49 的新二进制**）+ 剪贴板引擎/面板 + 桌面服务×2。Host / Tray / Watchdog / Agent **都没跑**（这是对的） |
| **下一步（两条并行）** | ① **人工走查 core 托盘菜单**（core 已部署，立刻能做）；② **解开 B1**（`packages/` + `host/` 的归属）。**在此之前不要进 S6** |

**动手前必须知道的三件事**（各自展开在 runtime-facts）：

1. **`✅` 有四种含义**（源码 / 构建 / 部署 / 真机），过去绝大多数 `✅` 只是前两种。→ runtime-facts §7
2. **有五件事不能做**（起旧 Host、手拷 DLL、杀哨兵、把单测绿当功能对、B1 前动 S6）。→ runtime-facts §8
3. **别信 CLI 做的验证**：部署的 CLI 是 09-18 旧构建（`--core` 返回 Usage=2）。→ runtime-facts §1

---

## 1. 现在唯一该做的事：解开 B1

**B1 的实测边界（不是推算）**：8 个发布组件里，**只有 3 个能从干净检出构建**
（`tray` / `recovery` / `updater`；`launcher` 2 个 `ProjectReference`、`host` 25 个、`Cli` 4 个全部指向未提交的 `packages/`）。

⇒ B1 卡在同一件事上三次：**`packages/` 那批未提交工作（380 项）+ `host/` 的归属**。三条依赖与"无解时的备选"见计划 §7.2。

**解开之后的顺序（计划 §7.3，不要跳）**：
`publish.ps1`（验 8/8 产物）→ 部署 core → 真机验 S4-5 清单 → 走查 S5-5 卸载 → 点 core 托盘 8 个动作
→ 核销表 1 → **只有全绿才动 S6**。

---

## 7. 指针：谁是现行、谁已过期

| 文档 | 状态 |
|---|---|
| [`2026-09-20-runtime-facts.md`](2026-09-20-runtime-facts.md) | ✅ **现行 · 本文件的另一半**（实测快照 / 单拷部署流程 / core 验证记录 / 完成度四级定义 / 操作禁忌） |
| [`docs/plans/2026-09-18-on-demand-core-architecture.md`](../plans/2026-09-18-on-demand-core-architecture.md) | ✅ 现行（完整计划 + §13.x 真机记录 + §13.23 对账三表）。**注意它内部有三代状态**：§7 主表 → §7.1（09-19 晚快照）→ §7.2/7.3/7.4（最新可执行边界）。**以 §7.2/§7.3 为准** |
| [`六层模型与未来扩展点.md`](六层模型与未来扩展点.md)、[`2026-09-20-current-architecture.md`](2026-09-20-current-architecture.md) | ✅ 现行（架构模型骨架 / 拓扑与进度快照） |
| `docs/coding-standards-core-rust.md`、`docs/threat-model.md`、`docs/testing.md`、`docs/known-exceptions.md` | ✅ 现行 |
| `docs/2026-09-11-resident-architecture.md` | ❌ **已被取代**（方向相反：那份说"除 Dock/菜单栏外全部常驻"） |
| [`entry-point-map.md`](entry-point-map.md) | ⚠ **过渡期现状图**（描述正在被拆掉的网状结构），S6 落地后须重画 |

## 8. 交接与收工

- 交接：读本文件 → `AGENTS.md` → 计划 §7.2/§7.3（**不要**从 §7 主表开始读）→ 跑门禁基线。
- 收工：门禁全绿 → 决策记录补全 → 更新本文件与 runtime-facts → 提交带范围前缀。
