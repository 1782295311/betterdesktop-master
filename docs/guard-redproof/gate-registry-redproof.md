# gate-registry-redproof.md — 门禁变红物证

> 门禁：`verify-gate-registry.ps1` · 取证日期：2026-08-20 · 取证方式：真实 git 注入提交 + 撤销提交
> 取证环境 E1–E3：**N/A**（纯文档门禁，无构建产物）；E4：注入前 HEAD = `待填写`；注入 commit = `待填写`；撤销 commit = `待填写`

## ① 可复现的注入片段

```
待填写：注入一个未登记的 verify-*.ps1 脚本

```

违规构造：scripts/ 下存在 verify-*.ps1 脚本，但未登记进 run-gates.ps1 注册表。

## ② 原样拷贝的失败输出

待填写：运行 gate-registry 门禁的失败输出（退出码 1）。

## ③ 撤销证明与恢复验证

待填写：撤销注入后重跑门禁的通过输出（退出码 0）。

**结论：该门禁红/绿双向验证待完成。**
