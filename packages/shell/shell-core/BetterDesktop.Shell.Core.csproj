<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <UseWPF>true</UseWPF>
    <RootNamespace>BetterDesktop.Shell.Core</RootNamespace>
    <AssemblyName>BetterDesktop.Shell.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\kernel\kernel\BetterDesktop.Kernel.csproj" />
    <ProjectReference Include="..\..\api\BetterDesktop.Api.csproj" />
    <!-- 热键解析唯一来源（HotkeySpec）：注册表复用同一解析器，禁止另写（两边各写一遍必然漂移）。 -->
    <ProjectReference Include="..\shell-clipboard-ipc\BetterDesktop.Shell.Clipboard.Ipc.csproj" />
    <!-- 热键解析唯一来源（HotkeySpec）：注册表复用同一解析器，禁止另写（两边各写一遍必然漂移）。 -->
    <ProjectReference Include="..\shell-clipboard-ipc\BetterDesktop.Shell.Clipboard.Ipc.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="BetterDesktop.Shell.Core.Tests" />
  </ItemGroup>

</Project>
