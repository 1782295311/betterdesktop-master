# Better Desktop Cordis 项目理解与问题清单

## 一、项目定位（理解）

`better-desktop-cordis` 是 Windows 桌面外壳（Cairo Shell 风格）的从零重写，采用 **Cordis 风格插件内核**（C# 复刻）+ WPF 外壳 + 薄宿主。结构：

- **内核层 `packages/kernel/`**：`kernel`（服务图 / 插件运行时 / 事件总线）、`kernel-hmr`（双 ALC 热重载）、`kernel-loader`（cordis.yml 加载）、`  kernel-timer`（定时）。
- **外壳层 `packages/shell/`**：`shell-core`（毛玻璃/窗口基类/桌面表面）、`shell-app-source`（开始菜单/注册表扫描）、`shell-dock`（Dock 窗口、固定项、应用提取器、运行项检测）。
- **宿主 `host/`**：薄 exe，装配内核并加载插件树。
- **治理**：`scripts/` 有一整套门禁（run-gates.ps1 + 各 verify-*.ps1 + Pester 测试），文档体系完整（`AGENTS.md`/`docs/`/`.agents/notes/`）。

总体评价：**工程纪律（文档、门禁、决策记录）相当成熟，但业务/插件实现存在若干真实缺陷与半完成接线**，下面按严重度归类。

---

## 二、发现的问题

### A. 并发 / 线程安全（真实缺陷，建议优先修）

1. **HmrManager 双锁不互斥**（`packages/kernel/kernel-hmr/HmrManager.cs`）
   - `_gate`（同步 `lock`）与 `_operationGate`（`SemaphoreSlim`）保护同一份 `ManagedPlugin` 状态，但两者互不互斥。`GetPluginStatus` / `GetPluginRuntimeInfos` 用 `_gate`，而 `LoadPluginAsync` / `ReloadPluginAsync` 在 `_operationGate` 下写 `managed.Status` / `.Module` / `.Handle`。二者可并发 → 读到撕裂的 `ManagedPlugin`。

2. **PluginHandle 重启竞态**（`packages/kernel/kernel/Core/PluginHandle.cs`）
   - `OnServiceChanged` 与 `RestartAsync` 都是 `_current = RestartCoreAsync()`（fire-and-forget），没有任何串行化。连续多次服务变化会让多个 `RestartCoreAsync` 并发跑：旧任务还没 `Unload` 就被新任务覆盖 `_current`，导致双 Unload / 双 Load、effect 清理丢失。
   - 另：`StartAsync` 的「Pending 早退」判断与 `_current` 复制不在同一原子段（`lock` 内读 `_state`，`lock` 外写 `_current`），理论上可重复启动。

3. **`CordisContext.ActiveFiber` 是上下文级单字段**（`CordisContext.cs` / `PluginHandle.cs`）
   - `ReloadAsync` 期间设置 `_context.ActiveFiber = this`。若同一上下文被并发加载多个插件（不经 HMR 的 `_operationGate` 串行化），effect 会被错误归属到错误的 fiber。当前 HMR 有串行化兜底，但 `CordisContext` 本身不保证线程安全，直接 `context.Plugin(...)` 调用有风险。

### B. 资源泄漏 / 生命周期

4. **`CordisContext._plugins` 永不移除已卸载 handle**（`CordisContext.cs`）
   - `Plugin()` 写入 `_plugins`，`PluginHandle.DisposeAsync()` 只把状态置 `Disposed`，不把自己从 `_plugins` / `_children` 移除。每次 HMR 重载会累积僵尸 handle，`NotifyDependents` 每次遍历全部（含已销毁）。`_children` 同理。

5. **ALC 回收依赖经典陷阱**（`kernel-hmr/AssemblyPluginSource.cs` / `PluginModule.cs`）
   - 卸载靠 `PluginModule.DisposeAsync` → `loadContext.Unload()`，但 `PluginModule` 仍持有 `IPlugin` 实例引用直到 GC。若宿主/缓存保留了 plugin 引用，ALC 无法真正卸载。当前无弱引用或句柄释放校验。

### C. 契约 / 文档不一致

6. **`ShellCorePlugin.Inject` 漏声明依赖**（`packages/shell/shell-core/ShellCoreRumin plugin.cs`）
   - `Inject` 只有 `IWindowHandleService`，但 `LoadAsync` 实际用 `context.Get<IVibrancyService>()!`（非空断言）。缺失声明 ⇒ 若 VibrancyService 晚于 ShellCore 提供，不会触发依赖驱动重载；一旦缺失直接 NRE 崩溃。应把 `IVibrancyService` 加入 `Inject`。

7. **文档与实现漂移**
   - 架构文档（`docs/architecture.md`）称 Effect 卸载「逆序**并行**执行」，但 `PluginHandle.UnloadInternalAsync` 是 `foreach` **顺序**执行（仅逆序，非并行）。
   - `LoadPluginAsync` 在「已加载」分支返回 `PluginLoadResult(..., success:false, status:Loaded, ...)`，`success=false` 却带 `Loaded` 状态，语义自相矛盾，调用方易误判。
   - `IsAbiCompatibleWith`（`kernel-hmr/SemanticVersion.cs`）仅比对 `Major` + `>=`，未校验 Minor/Patch 破坏性变更（按 SemVer 约定 Minor 可兼容，但内核 Minor 破坏性改动会被误判为兼容）。

### D. 占位 / 未完成（功能缺口）

8. **DockWindow 接线半完成**（`shell-dock/DockWindow.xaml.cs` / `DockPlugin.cs`）
   - 构造函数签名 `(IVibrancyService, object _animation, object _dockService, IDockAppsService, IDockIconService, object _dockLayoutService, DockPlugin?)`，其中 `_animation`/`_d *ockService`/`_dockLayoutService` 为 `object` 且**构造体内从未使用**，`DockPlugin` 以 `new object()` 占位传入。说明动画服务、dock 服务、布局服务尚未真正接入。

9. **`AdvancedVibrancyService` 是空壳**（`shell-core/Vibrancy/AdvancedVibrancyService.cs`）
   - `ApplyDynamic` / `ApplyWithEdgeGlow` / `UpdateDynamicBlur` 仅把参数存进字典，**从未实际应用到窗口**（注释：「这里可以添加实际的动态模糊实现 / 目前使用基础的毛玻璃效果」）。所谓「高级毛玻璃」目前只有基础 `VibrancyService.Apply` 生效。

10. **WPF 线程风险**（`shell-dock/DockPlugin.cs`）
    - `LoadAsync` 直接 `new DockWindow().Show()`。若 `context.Plugin(...)` 在后台线程（非 Dispatcher/STA）被调用，WPF 会抛 `InvalidOperationException`。需确认宿主调用线程，否则必崩。

### E. 健壮性

11. **EventBus 结果语义歧义**（`kernel/Core/EventBus.cs`）
    - `BailAsync` / `WaterfallAsync` 用 `result is not null` / `EqualityComparer<TResult>.Default.Equals(result, default)` 区分「无结果」与「有效结果」。若 handler 合法返回 `default`（如 `0`、空串、默认值结构体），会被误判为「未处理」而被跳过。应改用显式 bool 或 `Result<T>` 包装。

12. **`AppSourceService._seen` 并发与一致性**（`shell-app-source/Services/AppSourceService.cs`）
    - `LoadSeen()` 每次调用清空并重载共享 `_seen` HashSet，且无锁；若后台定时器与手动调用并发，可能丢失「已见」写入。另外「首次运行」与「快照文件损坏」无法区分，均会清空重复提示。

---

## 三、建议优先级

- **P0（必修）**：A1/A2/A3 并发竞态、B4 僵尸 handle 泄漏、C6 ShellCore 依赖声明。
- **P1**：B5 ALC 回收校验、C7 文档/契约修正、D8/D9 占位与空壳、D10 线程风险。
- **P2（后续）**：E11/E12 健壮性、C9 ABI 判定细化。

## 四、备注

- 本次为**只读分析**，未执行构建/测试（项目已含大量 bin 产物，说明此前可编译）。如需，我可接着跑 `dotnet build` 做编译级验证并补充「是否编译通过」的结论。
- 以上结论均基于实际读取的源码文件，未做猜测；每条都给出了具体文件路径与行内逻辑依据。
