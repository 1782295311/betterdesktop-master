# better-desktop-cordis 右键菜单问题审查报告

> 方法：借用「技术力积累」的复合功能深度拆解思路——先列子关注点，再逐条核对「正确性不变量（生死线）」，产出"什么做法一定会错"的问题清单。不做资产入库。
> 审查范围：`packages/shell/shell-context-menu`（统一菜单服务）、`shell-desktop` 消费端、`shell-dock`/`shell-start-menu` 旧路径。
> 日期：2026-09-02

## 一、复杂度判定

右键菜单是**复合功能**，命中全部四个判定信号：
① 多窗口生命周期（弹层窗口 + 子菜单 Popup + 宿主桌面窗口）；② 与 OS 强交互（SetParent 嵌入 child window、HKCR 注册表探测、ShellExecute、SendTo）；③ 多模式路径（新路径 `context-menu.migrated` / 旧自绘路径回退；shell 命名空间 vs 文件系统）；④ 依赖环境状态（DPI、多显示器、主题令牌）。

按复合功能标准，必须核对生死线。核对结果如下。

## 二、已守住的生死线（✅，确认无问题）

| 生死线 | 证据 |
|---|---|
| 模板/样式禁用 `FrameworkElementFactory.SetValue(dp, ProvideValue(null))`，一律 `XamlReader.Parse` | MenuStyling.cs 全部走 XamlReader.Parse，含踩坑注释 |
| 弹层窗口前景必须显式挂 ThemeForeground（否则深底黑字"纯黑"） | MenuHost.BuildPanel 87 行 `SetResourceReference` + 注释 |
| 嵌入 child window（WS_CHILD 桌面）中不可用 WPF Popup/ContextMenu → 新路径改独立 Topmost 窗口 | MenuHost.cs 33-37 行设计注释 + ContextMenuPopupWindow 实现 |
| 新旧双路径必须互斥（否则右键双菜单） | DesktopIconsControl 111-121/687-694：`_useMenuService` 门控，新路径下 `cell.ContextMenu` 不赋值 |
| 贡献项/命令单项失败不拖垮整个菜单 | MenuService.BuildItems try-catch 逐贡献者隔离；MenuHost.Fill 点击 catch 单命令 |
| 过滤后悬空分隔线清理 | FilterCapabilities → TrimEdges（首/尾/连续重复） |
| 右键未选中项先单选再弹菜单（explorer 同款） | OnMenuServiceMouseUp 1575-1579 |
| 坐标换算：PointToScreen 物理像素 ÷ PixelsPerDip → DIP | ShowMenuAsync 1617-1620（多 DPI 屏正确） |

## 三、问题清单

### P1 · 生死线违反（会实际出错）

**P1-1 多选操作被静默降级为单选** `[verified]`
- 位置：`DesktopIconsControl.cs` 1646-1662（`InvokeBrowserCut/Copy/Delete`）；`DesktopMenuTemplates.cs` 191-207。
- 问题：三个命令一律 `SetSelection([path])` **重置**选中集再执行。用户框选/多选后右键其中一个图标（选中集保持不变，走 explorer 惯例）点"删除"——**只删右键那一个**，其余选中项原地不动，无任何提示。
- 关联证据：`MenuRequest.SelectedPaths` 注释写"多选时能力过滤取交集"，但桌面路径从未消费它（唯一消费者是 UserMenuContributor 的逐路径执行）；`FileClassifier.ClassifyMany`（交集能力过滤，专门为多选写的）**全库零调用，是死代码**。契约与实现漂移。

**P1-2 多显示器定位错误：菜单被拉回主屏** `[verified]`
- 位置：`ContextMenuPopupWindow.cs` 45-47（Loaded 钳制）。
- 问题：用 `SystemParameters.WorkArea`（**仅主显示器**的工作区，且 DIP 换算按主屏 DPI）。在副屏（尤其位于主屏左侧、坐标为负的副屏）右键，`Math.Clamp` 会把菜单位置强行夹回主屏工作区——菜单跳屏、出现在远离鼠标的位置。副屏 DPI ≠ 主屏 DPI 时钳制数学也是错的。
- 修复方向：按 `screenPos` 所在屏取 `Screen.FromPoint`（WinForms 互操作或 MonitorFromPoint）→ 该屏工作区矩形（物理→DIP）再钳制。

### P2 · 语义/行为缺陷

**P2-1 Esc 直接关闭整窗，不先收子菜单** `[verified]`
- 位置：`ContextMenuPopupWindow.cs` 34-40。`PreviewKeyDown` 在隧道阶段先于 MenuItem 的 KeyDown 处理触发，`Close()` 关掉整个菜单。Explorer 语义是 Esc 先收一层子菜单，再 Esc 才关整个菜单。方向键导航子菜单时的 Esc 行为与系统习惯不一致。

**P2-2 命令"先执行后关窗"，依赖隐式时序** `[verified]`
- 位置：`MenuHost.cs` 134-146。`def.Command?.Invoke()` 同步执行**之后**才 `window.Close()`。
- 后果：① 命令里若有同步弹窗/阻塞操作（Process.Start 同步等待、模态框），菜单窗口滞留屏幕直到命令返回；② "重命名"场景恰好**依赖**这个时序（Command 内 `box.Focus()` 使菜单失焦 → Deactivated → Close）——这是巧合契约，谁把命令改成异步/延迟执行，重命名入口就坏。
- 修复方向：先 `execute(def.Id)`（关窗）再 `Dispatcher.BeginInvoke` 执行命令；重命名场景改为显式驱动。

**P2-3 虚拟项/回收站菜单语义错误** `[verified]`
- 位置：`FileClassifier.cs` 79 行给 `FileKind.InRecycleBin` 授 `Open` 能力 → `DesktopIconTemplate` 对回收站内文件显示默认项"打开"（InvokeOpenEntry 对已删除对象会失败）；`DesktopIconTemplate` 216-230 行：`发送到` 子菜单对 shell 命名空间项（回收站/此电脑，路径 `::{CLSID}`）也渲染，SendTo 目标程序收到 `::{...}` 参数多半无法处理。
- 修复方向：InRecycleBin 默认项应为"还原"；`发送到`/`打开文件位置` 等文件系统能力对 `entry.IsShellNamespace` 隐藏。

**P2-4 文档承诺"过长由渲染层截断"未兑现** `[verified]`
- 位置：`MenuItemDef.cs` 12 行注释 vs `MenuHost.Fill`（无截断）+ `MenuStyling.ItemStyleXaml`（无 TextTrimming/MaxWidth）。
- 后果：超长文本（如用户自建 sendto 快捷名、自定义菜单名）无限撑宽菜单，可溢出屏幕。契约注释即红线，红线未落实。

**P2-5 旧路径双标准 + Popup 红线自查缺口** `[verified]`
- 位置：`shell-dock/DockWindow.xaml.cs` 891/1007/1456、`AppGrabberWindow.cs` 781；`shell-start-menu` 各 Layout + `AppItemActions.cs` 15。
- 问题：dock/开始菜单共 10+ 处仍用原生 `new ContextMenu()`——与统一服务的主题令牌、能力过滤、用户自定义项三套能力全部脱节（视觉/行为双标准）。更重要的是：MenuHost.cs 自己声明的红线"嵌入 child window 中 Popup/ContextMenu 不可靠"——若 dock/开始菜单窗口与桌面同属嵌入层级，这些原生 ContextMenu 命中的正是同一条红线；若是独立顶层窗口则无此险。**判定依据（窗口层级）未写进任何文档**，迁移时无从自查。

### P3 · 小缺陷/契约缺口

- **P3-1** `MenuService.Dismiss()` 105-110 行裸读 `_active` 字段（`DismissInternal` 用了 Interlocked，这里没有）——竞态窗口极小但不一致。`[verified]`
- **P3-2** Loaded 才做边缘钳制，窗口先在默认位置（Manual+NaN → 系统默认）闪现一帧再跳到目标位置。`[inferred]`
- **P3-3** `HasWindowsTerminal` 只探测 `%LOCALAPPDATA%\Microsoft\WindowsApps\wt.exe`（MSIX 版）；winget/ scoop 等安装的 wt 不命中 → "在终端中打开"静默消失（隐藏优先的副作用，可接受但应知情）。`[verified]`
- **P3-4** 顶层（非子菜单）Toggle/Radio 项的 IsChecked 在当前 ItemStyleXaml（未重写 Template）+ TopLevelItem 角色下**无默认勾选列可渲染**。当前模板恰好把所有 Radio/Toggle 都放子菜单里（子项角色有勾选列）所以未暴露，但契约缺口在：任何贡献者/模板未来加顶层 Toggle 都会静默丢失勾选态。`[inferred]`
- **P3-5** `FilterCapabilities` 在 `identity is null` 分支直接返回、跳过 TrimEdges——当前无实害（无能力过滤就无悬空分隔线），但两条路径不对称，防御性缺口。`[verified]`
- **P3-6** `UserMenuContributor` WorkingDir 用 `Trim('"')` 除引号是补丁式处理（路径含嵌套引号即破）；与 Arguments 的正则校验精细度不对称。`[verified]`

## 四、修复优先级建议

1. **P1-1 多选降级**：`InvokeBrowser*` 改用 `_browser.SelectedPaths`（右键项在选中集内时整集操作）——最小改动即恢复 explorer 语义；顺手让 `ClassifyMany` 进入多选路径（能力取交集）。
2. **P1-2 多屏钳制**：按屏幕点所属屏取工作区。
3. **P2-2 命令时序**：先关窗后执行（异步 BeginInvoke），重命名场景显式化。
4. 其余按 P2 → P3 排入迭代。

## 五、审查中确认的"实现亮点"（避免误伤）

- 五区块组装 + 组间分隔线 + TrimEdges 的管线设计清晰，防漂移有显式 `GroupOrder`。
- 贡献者异常隔离、Opening 订阅者异常隔离，符合"单项失败不拖垮整体"。
- 双路径互斥门控正确，不存在双菜单回归。
- 所有主题绑定走 XamlReader.Parse / SetResourceReference，未重蹈 FrameworkElementFactory 红线。

## 六、修复记录（2026-09-02，同日闭环）

| 问题 | 修复 | 位置 | 状态 |
|---|---|---|---|
| P1-1 多选降级 | `InvokeBrowserCut/Copy/Delete`：右键项在选中集内→整集操作，不再重置选中集；`ShowMenuAsync` 多选走 `ClassifyMany` 交集能力过滤（死代码转正，契约兑现） | DesktopIconsControl.cs | ✅ 已修复 |
| P1-2 多屏钳制 | 弹层 Loaded 改为：MonitorFromPoint 找所在屏 → GetMonitorInfo 取该屏工作区（物理域）→ `SetWindowPos` 物理像素落位；DIP↔物理用该屏 GetDpiForMonitor 反算（对齐 MenuBarScreen 口径），规避 WorkArea 主屏局限与 Left/Top 跨 DPI 域歧义 | ContextMenuPopupWindow.cs | ✅ 已修复 |
| P2-1 Esc 收子菜单 | PreviewKeyDown 仅在无展开子菜单（`HasOpenSubmenu` 遍历视觉树）时关整窗，否则放行给 MenuItem 先收子菜单 | ContextMenuPopupWindow.cs | ✅ 已修复 |
| P2-2 命令时序 | 点击先 `execute`（关窗），命令经 Dispatcher(Input) 延后执行；重命名从"失焦巧合"转常规时序 | MenuHost.cs | ✅ 已修复 |
| P2-3 虚拟项语义 | FileClassifier 回收站身份去 Open 能力；模板回收站不显示"打开"；发送到移入 `!IsShellNamespace` 分支 | FileClassifier.cs / DesktopMenuTemplates.cs | ✅ 已修复 |
| P2-4 截断契约 | ItemStyleXaml 增加 `MaxWidth=280`，兑现 MenuItemDef"渲染层截断"承诺 | MenuStyling.cs | ✅ 已修复 |
| P3-1 Dismiss 竞态 | 改用 `Interlocked.Exchange` 与 DismissInternal 同口径 | MenuService.cs | ✅ 已修复 |
| P3-5 过滤不对称 | identity==null 分支同样走 TrimEdges | MenuService.cs | ✅ 已修复 |
| P2-5 旧路径迁移 | **第二轮（2026-09-02 晚）已完成**：新建 `MenuSurface`（统一右键接入点）+ `IMenuService.ShowAsync(items, pos)` 低层入口；dock 4 处（WinX 降级/系统区/图标/应用提取器）+ 开始菜单 10 处（程序行/树/搜索结果/磁贴/所有应用/两处电源菜单）全部迁统一弹层；`DesktopMenuStyling` 样式委托 `MenuStyling` 唯一权威源；重命名框抑制 WPF 默认剪贴板菜单 | shell-dock / shell-start-menu / shell-desktop | ✅ 已修复 |
| 原生菜单抑制 | 检查项：①自绘表面右键全部 e.Handled；②桌面窗口 HWND_TOP 覆盖 DefView（既有）；③文本编辑框保留剪贴板菜单（功能非缺陷）；④跨进程子类化 DefView **否决**（explorer 进程窗口，改其 WndProc 会崩溃宿主）。开始菜单失焦自毁与弹层抢激活的冲突经 `IMenuService.IsOpen/Closed` 豁免机制解决 | IMenuService / StartMenuWindow | ✅ 已修复 |
| P3-2/3/4/6 | 低风险/契约缺口类，未在本轮处理 | — | ⏳ 待办 |

### 第二轮新增生死线（统一收口）
1. **禁止再 new WPF ContextMenu()**：任何表面的右键/按钮菜单一律 `MenuSurface.Attach`（右键）或 `IMenuService.ShowAsync(items, pos)`（按钮触发），渲染经 MenuHost（ShellWindow 弹层 + MenuStyling 令牌）。[verified]
2. **失焦自毁型宿主豁免**：宿主若 Deactivated 即自毁（开始菜单），Deactivated 时须查 `menus.IsOpen` 豁免，并挂 `Closed` 补收尾——否则弹层抢激活导致宿主消失、弹层浮空。[verified]
3. **坐标换算统一走 `MenuSurface.GetScreenPos/AtCursor/BelowOf`**（物理 ÷ 源元素 PixelsPerDip）。[verified]
4. **ContextMenuManager 参考结论**：它管"Windows 原生菜单内容"（注册表：HideBasedOnVelocityId/LegacyDisable/减号前缀备份/Shell Extensions\Blocked，全可逆），进程内抑制需自建；其注册表枚举/禁用模型可作为未来"shell 扩展管理器"功能的蓝本。[verified]

构建：全仓 0 错误（补 start-menu/dock → shell-context-menu 项目引用）；host 已启动验证。

## 七、第三轮（真机反馈闭环，2026-09-02 晚）

用户真机反馈四问题，全部取证后修复：

| 反馈 | 取证 | 根因 | 修复 |
|---|---|---|---|
| 窗口纯黑 | 日志埋点：transp=True、PopupBackground=#FF2C2C2E 命中、尺寸正常——令牌没问题 | `ShellWindow.ApplyAppearance` 在 MaterialChanged 时**绕过 ApplyWindowMaterial 空重写**直接调 VibrancyService.Apply → DWM accent 打在 AllowsTransparency 分层窗口上 = 整窗纯黑（layered window 不支持 DWM accent 合成） | 弹层构造期 `VibrancyService = null` 短路基类材质路径 |
| 位置飘忽 | 每次菜单都在默认位置闪现后跳到目标 | Loaded 后才钳位（先 Show 后跳） | 构造期预测量面板 DesiredSize，`SourceInitialized`（可见前）一次性 SetWindowPos 落位+定尺寸 |
| 消失逻辑麻烦 | — | 窗口 Background=Transparent 使圆角四角区域参与命中：点"外面"若落在四角则点击被吞 | Background=null（非面板区域不命中，点击穿透） |
| 功能少/识别不出属性 | 日志：FileKind 识别**正常**（File/Unknown/Archive/SourceCode/Document 全出），items=9 固定 | 是模板项真的少，非识别缺陷 | 图标菜单补强：关联文件"打开方式…"、文件夹/驱动器"在新窗口中打开"、"复制文件地址"、shell 虚拟项（此电脑/回收站）补"打开" |
| 设置里看不到规划 | — | 无分区 | 新增「右键菜单」设置分区（ContextMenuSection 经 ISettingsSectionRegistry 注册）：15 项功能状态清单（✅9 已实现 / 🔧6 已计划）+ context-menu.migrated 回退开关 + 自定义项配置说明 |

**关键更正**：用户感知的"识别不出操作对象属性"实为"菜单项数量少"——识别层（FileClassifier）工作正常，证据为日志 file=File/Archive/SourceCode 等正确输出。真正的功能缺口在模板层（已补）与 Shell 扩展 verb 透传层（COM IContextMenu 集成，列 🔧 计划）。

## 八、第四轮（截图实证：两菜单并存，2026-09-02 深夜）

用户截图：Win11 原生桌面右键菜单与统一弹层**同时出现**（原生菜单盖住统一菜单上半截）。日志取证：`窗口层右键 up`（WPF 收到）与 `Show scope=Desktop items=11` 同刻出现——右键同时到达两个菜单系统。暴露 2 个 bug：

**Bug A · WM_CONTEXTMENU 冒泡通路（原生菜单并存）** `[verified]`
- 通路：桌面窗口 WS_CHILD 嵌入 explorer 桌面。WPF 层处理了右键（MouseRightButtonUp → IMenuService），但 **WPF 不吞 WM_CONTEXTMENU**——DefWindowProc 把未处理的 WM_CONTEXTMENU 转发给父窗口（explorer 桌面）→ explorer 弹原生菜单。旧路径靠 ContextMenu 附加属性恰好消费了 WM_CONTEXTMENU，迁移后该消息无人处理即冒泡。
- 修复：DesktopWindow hwnd hook 一律 `handled=true` 吞 WM_CONTEXTMENU（覆盖子窗口树转发链）。

**Bug B · 弹层定位被 Show 覆盖（位置飘忽真身）** `[verified]`
- 根因：SourceInitialized 里 SetWindowPos 的成果会被 WPF Show 流程用 Left/Top（NaN → CW_USEDEFAULT 级联位置）**重设冲掉** → 菜单弹在随机级联位置（截图里统一菜单偏离光标左下方即此）。
- 修复：改为写 WPF 属性域（Width/Height=预测量值，Left/Top=钳制后物理 ÷ 窗口 DPI），Show 不会覆盖非 NaN 属性；删除 SetWindowPos 路径。

两修复后构建 0 错误，host 已重启。

## 九、第五轮（三症状一体：黑柱实证，2026-09-02 深夜）

用户反馈：①"新建"点击没反应；②样式还是纯黑；③**呼出次数越多越难唤出，乃至唤不出**。日志取证实锤：`菜单窗口 size=214x757`（正常 176x251 的 **3 倍高**）——上一轮"构造期预测量定尺寸"（panel.Measure → 手设 Width/Height）返回了异常尺寸，一条顶天立地的深色窄柱 Topmost 叠在桌面上，**三个症状统一解释**：黑柱=纯黑观感；子菜单内容挤出屏幕=新建"没反应"；Topmost 长柱拦截桌面右键=越唤越难唤出。

修复：
1. **尺寸回归 `SizeToContent.WidthAndHeight`**（22:34 旧构建实测正常 176x251），废弃预测量路线；位置两段钳制（SourceInitialized 估算尺寸 / Loaded 真实尺寸精调，只写 Left/Top）。
2. **子菜单 Popup 自建模板**（MenuStyling.CreateSubmenuStyle）：WPF 默认 MenuItem 下拉 Popup 背板是系统 SystemColors（深色系统=黑块）——"点击新建没反应"的第二个真因（子菜单弹出但黑底不可辨）。自建模板 PART_Popup 内铺 PopupBackground/PopupBorder 令牌 + 右弹出 + 箭头。普通项也换自建模板：补 ✓ 勾选列（Radio/Toggle 此前不渲染）+ 悬停令牌 + 禁用降态。
3. **MenuService 会话生命周期**：菜单关闭时 `CompareExchange` 清 `_active`——此前窗口已关而 `_active` 未清 → `IsOpen` 恒 true → 开始菜单的失焦豁免被永久卡住（开始菜单收不起的潜在根因）。

构建 0 错误，host 已重启（PID 47736）。

## 十、第六轮（交互定版：悬停展开 + 材质恢复，2026-09-02 深夜）

用户定版两条：①二级菜单应**悬停自动展开/收回**，不需要点；②菜单纯黑**不是统一窗口基类该有的样子**。

**修复 1 · 子菜单悬停交互**：新建 `MenuHost.WireSubmenuHover`——悬停宿主项自动展开并收回同级其它子菜单（同级互斥）；移出宿主/弹层延迟 350ms 收回（留移入缓冲）；移入弹层取消收回。不依赖 MenuItem 角色内建逻辑（我们的项挂在 StackPanel 而非 Menu 下，内建悬停展开不可靠——实测"点不开"）。点击兜底按 `Role` 分流：非 header 角色才手动开合，防与内建开合叠加抵消。

**修复 2 · 材质恢复**：撤销上一轮 `VibrancyService = null` 短路——dock/设置窗口同为分层窗口 + vibrancy 材质且渲染正常，证明"材质致黑"推断不成立（黑感实为 214x757 异常尺寸黑柱 + 深色令牌观感）。弹层恢复基类 ApplyWindowMaterial 路径，与所有 ShellWindow 同一毛玻璃观感（"统一窗口基类该有的样子"）。

构建 0 错误，host 已重启（PID 47492）。

## 十一、第七轮（死锁自愈 + 玻璃观感，2026-09-02 深夜）

用户反馈：①同一位置多次右键后呼唤不出；②还是看不到统一基类的影子（无玻璃观感）。

**修复 1 · 两条死锁通路自愈**（"多次右键后唤不出"的机制）：
- **吞右键死锁**：菜单打开时盖住光标，同位置再次右键落在菜单窗口自己身上——无处理器无声吞掉，且不触发 Deactivated（无激活状态变化）→ 菜单永不自关、拦死桌面右键。修复：弹层 `PreviewMouseRightButtonUp` → 立即 Close（下一次右键重新呼出）。
- **未激活隐形拦截器**：ShowActivated 但系统可能拒绝激活（快速连续操作的前台锁），从未激活的窗口不触发 Deactivated → 永久隐形拦截。修复：Loaded 后 800ms 仍未激活 → 自愈关闭（日志埋点）。

**修复 2 · 半透明面板透出 vibrancy**：材质上一轮已恢复但被不透明 `PopupBackground` 全盖住。面板底色改为克隆画刷后 `Opacity=0.82`（禁止直接改共享画刷，会污染全局令牌）——毛玻璃从 dock 同款通道透出，"统一窗口基类该有的样子"。

构建 0 错误，host 已重启（PID 45296）。

## 十二、第八轮（透明度用户自调 + 快捷工具管理器，2026-09-02 深夜）

用户定版两条：①透明度应由用户自己选；②右键菜单管理的重点是**添加第三方快捷工具**（压缩软件、VS Code、WPS 等）。

**新增 1 · 菜单不透明度设置项**：`context-menu.opacity`（0.3~1.0，默认 82%），设置页「右键菜单 → 外观」滑块调节；MenuService 每次展示时读取传入 MenuHost → BuildPanel 克隆画刷后设 Opacity（透出 vibrancy 毛玻璃的程度用户可控）。改动即时生效（下次右键）。

**新增 2 · 快捷工具管理器**（设置页「右键菜单 → 快捷工具」）：
- **常见工具检测一键添加**：VS Code（两条安装路径）、7-Zip、WinRAR、WPS（注册表 App Paths 解析）、Notepad++——已安装的显示为"＋ 名称"按钮，点击即入列；已添加的自动隐藏。
- **手动添加**：文件选择对话框选 exe，名称自动取程序名，参数预填 `"%file%"`。
- **列表编辑**：每行 = 启用开关 + 名称 + 参数（占位符 %file%/%dir%/%desktop%/%name%）+ 作用域（文件右键/桌面空白）+ 删除；LostFocus 保存避免击键写盘。
- **存储与渲染同链路**：写 `context-menu.custom.items`（与 UserMenuContributor 同键），右键即时生效；此前手编 JSON 的条目也会出现在列表里。
- 「添加后对所有文件/文件夹显示」的按类型过滤列 🔧 计划。

实现链路：MenuService 注入 ISettingsService（读取透明度）→ MenuHost.Show 增 panelOpacity 参数 → BuildPanel 克隆画刷设透明度；ContextMenuSection 完整重写（外观/快捷工具/功能状态/控制四卡片）。

构建 0 错误，host 已重启（PID 40596）。

## 十三、第九轮（分类 + 图标，2026-09-03）

用户定版：工具**按功能区分**、**带图标**、图标在右键菜单里也要显示。

**实现：**
1. **UserMenuSpec 新增 `Category` 字段**；检测表全量带分类（压缩/编辑器/办公/播放器/图像/终端/开发/工具），App Paths 枚举项按 exe 名推断分类（`CategoryFor` 映射表）。
2. **菜单内归组**（UserMenuContributor）：≤3 个工具顶级直出；>3 个收进「快捷工具」子菜单，按 Category 分组——单项直挂、多项收分类二级子菜单。工具项带图标。
3. **图标全链路**：新建 `MenuIconCache`（SHGetFileInfo 提取小图标，无新包依赖，进程级缓存，Freeze 跨线程）；设置列表行 + 右键菜单项都渲染。`MenuItemDef.IconKey` 字段正式启用（约定 `tool:<exePath>`）。MenuStyling 两个模板加图标列（与勾选列共用 18px 槽位）。
4. **设置列表按分类分组**：分类标题 + 计数，行首图标。

构建 0 错误，host 已重启（PID 36476）。

## 十四、第十轮（排除法定版，2026-09-03）

用户定版："为了不麻烦用户，做排除法——第三方工具默认全开，不需要的在设置里关闭；右键菜单中显示对应工具图标。"

**模型反转（添加制 → 排除制）**：
1. **检测逻辑抽为共享 `ToolCatalog`**（Services 层，60s 缓存——右键高频触发不能每次跑注册表枚举）：精选候选 15 项 + App Paths 全量枚举 + 友名气映射 + 分类推断，按路径去重上限 60。
2. **UserMenuContributor 排除法合并**：每次 Build 把"检测到且不在排除名单且无同路径手动条目"的工具自动合成 UserMenuSpec（Id=auto.*，Scope=DesktopIcon）→ 走统一渲染链（图标/分类归组全部继承）。手动条目同路径时优先（可改名/改参数）。
3. **排除名单**：`context-menu.tools.disabled`（List<规范化路径>）。设置页"检测到的工具（默认全开）"按分类分组，每项 [图标][名称][开关]——关 = 加入名单、开 = 移出名单，即时生效。
4. **自定义添加**降为辅助入口（检测不到的程序），列表可编辑/删除。

生死线：排除名单按**规范化路径**比较（大小写/斜杠/尾斜杠归一）；检测缓存 60s（否则每次右键全盘扫注册表）。

构建 0 错误，host 已重启（PID 59872）。

构建验证：`BetterDesktop.Shell.Desktop.csproj` 与全仓 `BetterDesktop.slnx` 均 0 错误 0 警告（ExplorerTAP.dll 被 explorer 锁定未更新的警告与本次改动无关）。

