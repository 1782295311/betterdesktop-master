# better-desktop-cordis 人工审计报告（2026-09-08・定稿版）

> 审计方式：静态代码精读（
>
> **505 个 .cs 全部逐行精读完毕**
>
> ）+ 构建验证 + 全量测试 + 全量门禁 + Git 卫生检查 + 敏感信息扫描。审计期间未修改任何项目文件（本报告为唯一新增文件）。数据均为本次实测，非转引。本版相对首版的主要变化：① 全量逐行精读覆盖 505/505，撤除一切抽样表述；② 新增全量精读期发现（B6–B11、C8–C14）；③ 旧复核表中 D8/D9/E11/E12 由 "未复核" 转正式结论；④ **二轮独立复核（2026-09-08 晚间，另一次会话）**：重跑构建/测试/门禁与全部可测声明，逐条源码锚定 A/B 类发现并抽查 C 类，修正 6 处事实性错误（C6、C7 影响面、分包计数、项目数、D8 行数、C12 注释引用），详见第八节核验记录。

## 〇、全量逐行精读覆盖声明（本版核心）

全部 **505 个 .cs** 已逐行精读完毕，覆盖口径为 `kernel(71) + shell(403) + host(11) + watchdog(1) + recovery(1) + tools(7) + poc(11) = 505`（排除 `obj/`、`bin/`、`backups/`、`Temp/`、`TestResults/` 与全部非 .cs 文件）。分包明细：



| 包                                                                                                                | 文件数      | 精读状态                                                    |
| ---------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------- |
| kernel（Core/Contracts/hmr/loader/timer 源码）                                                                       | 71       | ✅ 逐行（含 4 个测试工程）                                         |
| shell-app-source                                                                                                 | 15       | ✅ 逐行（含 878 行 AppSourceService、349 行 ShellItemInterop；二轮核验修正：原文 13）   |
| shell-calendar                                                                                                   | 13       | ✅ 逐行                                                    |
| shell-context-menu                                                                                               | 38       | ✅ 逐行                                                    |
| shell-convert                                                                                                    | 33       | ✅ 逐行                                                    |
| shell-core                                                                                                       | 31       | ✅ 逐行                                                    |
| shell-desktop                                                                                                    | 13       | ✅ 逐行                                                    |
| shell-dock                                                                                                       | 28       | ✅ 逐行（含 2889 行 DockWindow.xaml.cs、1386 行 AppGrabberWindow；二轮核验修正：原文 24 文件 / 2601 行）       |
| shell-menu-bar                                                                                                   | 51       | ✅ 逐行（含 2072 行 MenuBarStatusStrip、980 行 WifiEnumerator）  |
| shell-music / shell-notification / shell-plugin-sdk / shell-pinning                                              | 2/4/1/5  | ✅ 逐行                                                    |
| shell-quick-note / shell-recent / shell-search / shell-settings                                                  | 3/5/8/21 | ✅ 逐行                                                    |
| shell-start-menu                                                                                                 | 25       | ✅ 逐行（含 952 行 Win10Layout、756 行 Win7Layout；二轮核验修正：原文 24）              |
| shell-status                                                                                                     | 41       | ✅ 逐行（含 1192 行 KeyboardLayoutInterop、215 行 StatusPoller） |
| shell-taskbar                                                                                                    | 10       | ✅ 逐行（含 1121 行 TaskbarAppearanceEngine）                  |
| shell-window-tracker                                                                                             | 12       | ✅ 逐行                                                    |
| shell 各测试工程（calendar/context-menu/convert/core/dock/menu-bar/notification/search/settings/status/window-tracker） | 44       | ✅ 逐行                                                    |
| host                                                                                                             | 11       | ✅ 逐行                                                    |
| watchdog / recovery                                                                                              | 1/1      | ✅ 逐行                                                    |
| tools（bamldecode/GdiAudit/ShellComponentsPlayground）                                                             | 4        | ✅ 逐行                                                    |
| tools/decomp（DockPlugin/FolderInputWindow/AppGrabberWindow）                                                      | 3        | ✅ 逐行（**第三方反编译参考转储**，非本产品源码，不计入产品质量结论，仅确认引用价值）           |
| poc（SystemStatusPoc）                                                                                             | 11       | ✅ 逐行                                                    |

> 二轮核验修正：表中 shell-app-source / shell-dock / shell-start-menu 实测为 15 / 28 / 25（原文 13 / 24 / 24），更正后各包合计恰为 shell(403)，与总分闭合（原文表格自加仅 396）；全仓非构建目录 .cs 共 **506** 个，其中 `docs/windows-shell-management/TaskbarAppearanceManager-草案.cs` 为设计草案（非产品源码），排除后即 505 覆盖口径——建议在覆盖声明中显式注明该草案排除理由，否则计数口径无法对外闭合。

全量精读期新增发现集中在 **shell-menu-bar / shell-status / shell-start-menu / shell-taskbar / shell-dock / shell-app-source** 六个包，详见第三节 B6–B11 与 C8–C14。

## 一、审计基线（实测结果）



| 项目                                       | 结果                     | 说明                                                                                           |
| ---------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------- |
| 解决方案构建 `dotnet build BetterDesktop.slnx` | ✅ 0 错误 / 0 警告          | 44 个项目构建成功（slnx 42 + 依赖 2：shell-app-source / shell-desktop），TFM 统一 net8.0-windows10.0.19041.0（二轮核验修正：原文「50 个项目」不成立，见 8.3） |
| 测试套件 `dotnet test BetterDesktop.slnx`    | ✅ 345 用例：345 通过 / 0 失败 | 初始 369/368/1 → A1 修复后 375 全过 → C15 删除 RegistryVerbs 引擎与专项测试后 345 全过（删 30 例） |
| 全量门禁 `pwsh scripts/run-gates.ps1`        | ❌ 4 红 / 1 跳            | package-readme、md-wrap、architecture-guard、dotnet-format 红；test-coverage 因依赖 dotnet-format 跳过 |
| 冒烟测试（门禁 smoke-test）                      | ✅ 通过                   | 主程序启动，进程存活 4 秒未崩溃                                                                            |
| STATUS.md 声称                             | 222 测试 / 221 绿         | 与实测 369/368 不符，文档已过时                                                                         |

## 二、门禁红灯明细（14 道门禁中 4 红 1 跳）



| 门禁                                                                                                                                | 状态 | 失败内容                                                                                                                                     |
| --------------------------------------------------------------------------------------------------------------------------------- | -- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| package-readme                                                                                                                    | 🔴 | 10 个测试工程（shell-\*-tests）缺 README.md；shell-app-source /shell-context-menu/shell-convert/shell-taskbar 四个包 README 缺 `## Known Limitations` |
| md-wrap                                                                                                                           | 🔴 | 约 60 个文件散文段落硬换行：含 ADR-002.md、overview.md、两份右键菜单审查报告、各包 README/DESIGN、docs/design-proposals、docs/performance、docs/plans/archive 等         |
| architecture-guard                                                                                                                | 🔴 | `host/Views/SplashWindow.xaml`—— 宿主携带业务 UI，违反 ADR-003 D2                                                                                 |
| dotnet-format                                                                                                                     | 🔴 | 17 个文件 using 导入顺序（IMPORTS IDE0005），集中在 shell-context-menu /shell-convert/shell-core/shell-dock/shell-menu-bar                            |
| test-coverage                                                                                                                     | ⏭️ | 依赖 dotnet-format 通过，被连带跳过                                                                                                                |
| 其余 9 道（agent-note /archived-notes/md-links/doc-budgets/gate-registry/host-log-sink/cross-asm-event/native-convergence/smoke-test） | ✅  | 全部通过；gate-registry 确认 14 个 verify 脚本全部登记且单测齐全                                                                                            |

门禁扫描范围缺陷：md-wrap 与 package-readme 递归扫描整个工作区，**不尊重 .gitignore**，把本机忽略目录（`packages/backups/`、`docs/plans/archive/` 等未跟踪目录）也计入违规。即使所有已跟踪文件修复，本地状态也会因忽略目录持续红灯。建议门禁改为基于 `git ls-files` 的跟踪文件清单扫描。

## 三、问题清单（按严重度）

### A. 立即处理（阻断收尾）

**A1・测试红 1 项，WIP 未收尾的实证**

`packages/shell/shell-convert-tests/ConversionMatrixTests.cs` 的 `Pdf源_Poppler目标登记` 失败：矩阵（`ConversionMatrix.cs:240-247`）已于 2026-09-07 为 .pdf 源新增 docx/xlsx（EngineKind.PdfText 文本提取）目标，但测试仍断言目标集恰为 `["png","jpg","txt"]` 且全部引擎为 Poppler—— 两处断言均已过期。这是「矩阵改了、测试没同步」的典型收尾缺口，也与仓库「收工前全绿」纪律直接冲突。

**A2・决策记录断裂（R7 违规）**

`.agents/notes/2026-09-08-pinvoke-t4t5-decisions.md`（提交 5d10c41 入库）在工作区被**删除且无归档、无替代**；`.agents/notes/` 活跃决策记录全部停留在 2026-08-20（P1 时代）。9 月以来三大功能批次（右键菜单 M1–M3 与 Win10 整改、shell-convert 全量、shell-calendar / 宿主菜单桥 / 内存治理器）均无 agent-note 决策记录，与 AGENTS.md「非平凡设计决策必须伴随决策记录」直接冲突（ADR-003 属例外，仅覆盖 HMR 追认 / 桌面归属 / TFM 三项）。

**A3・overview.md 混入内部会话残留**

已跟踪文件 `overview.md`（仓库根，2.6KB）除「内存治理器修正」记录外，文件尾部混有**内部代理会话的标记残留片段**（含内部标签与任务编号）。属 M11「敏感信息入库」红线触碰；且文件名与内容不符（该内容应为某次会话的工作记录，误存为 overview.md）。需清理内容或移入 `.agents/notes/`，并从 git 历史中清除。

### B. 架构与纪律（应尽快修复）

**B1・宿主膨胀，侵蚀 ADR-003 D2「薄宿主」边界**

`host/Bootstrap.cs` 已膨胀至 450 行：菜单命令桥分发（open-settings/dock-pin/toggle-key/toggle-desktop/convert/compress 等）、归档 / 转换执行、MessageBox 反馈、GetCursorPos P/Invoke 全部内联在宿主。host 还新增 `MenuService.cs`、`MenuCommandPipe.cs`、`ToggleKeyCommand.cs`、`DesktopToggleCommand.cs`、`FileLogSink.cs`、`Views/SplashWindow.xaml`。architecture-guard 门禁**只扫 host 下 .xaml**，对 .cs 业务代码无覆盖 —— 门禁存在盲区，宿主正在变回业务总控。

**B2・P/Invoke 收口回退**

`host/Bootstrap.cs:449` 新增 `GetCursorPos` DllImport，而权威层 `packages/shell/shell-core/Native/NativeMethods.cs:116` 已有同名声明（shell-dock、shell-desktop、shell-start-menu 均引用权威层）。这与最近两次「P/Invoke 收口至权威层」提交（d75a4f9、63028ca）方向相反，属收口纪律回退。native-convergence 门禁只做总量棘轮（当前 89/281，含本声明），不校验声明位置，收口靠自觉。

**B3・内存治理器注册键碰撞（真实缺陷）**

`PluginHandleSubject.Id`（`PluginHandleSubject.cs:24`）返回 `_handle.GetType().FullName`，即全部插件共享同一字符串 `BetterDesktop.Kernel.Core.PluginHandle`。而 `ResourceGovernor.Register`（`ResourceGovernor.cs:63`）按 Id 去重：**第一个插件注册后，其余全部被跳过**；`CordisContext.RemovePlugin`（`CordisContext.cs:137`）又用同一键注销 —— 任一插件卸载即清掉唯一的进程内监控对象。净效果：单插件内存评估 / 自愈实际只覆盖 1 个插件且极易被清空（进程级检测因全部插件采样同一 WorkingSet64 仍可用）。修复方向：PluginHandleSubject.Id 改用插件级唯一键（如插件 Name 或实例 GUID）。

**B4・第三方依赖无治理**

全仓 10 个 NuGet 包（ManagedShell 0.0.370 被 4 个 shell 包引用、LibreHardwareMonitorLib、Markdig、PdfSharpCore、ReverseMarkdown、SkiaSharp、[TinyPinyin.Net](https://TinyPinyin.Net)、System.Drawing.Common、YamlDotNet 15.1.6 与 16.0.0 双版本并存）**无任何 ADR 或决策记录背书**（ManagedShell 仅在已归档计划中提及）；无 `packages.lock.json` 锁定版本，M11/M12「依赖锁定与漏洞扫描」未落地。

**B5・门禁扫描不尊重 .gitignore**（详见第二节末）

**B6・KeyboardLayoutInterop TSF 路径双重 ReleaseComObject（新发现，真实缺陷）**

`packages/shell/shell-status/Native/KeyboardLayoutInterop.cs` 的 `GetActiveTsKlid` 与 `ActivateTsProfile` 中，对同一 RCW 连续调用两次 `Marshal.ReleaseComObject`：`profiles` 是 `(ITfInputProcessorProfiles)obj` 的接口强转，`obj` 为 `new TfInputProcessorProfilesClass()`（\[ComImport] class），两者指向**同一 RCW**。第一次 ReleaseComObject 后 RCW 引用计数归零即断开，第二次必然抛 `InvalidComObjectException`，被方法外层 catch 吞掉。净效果：`GetActiveTsKlid` 恒返回 null（TSF 活跃输入法识别路径实际从未生效，始终依赖 HKL/langid 降级匹配）；`ActivateTsProfile` 恒返回 false（TSF 即时激活输入法路径恒失败）。**历史注释 e40d64e 记录的「ITfInputProcessorProfiles::ActivateProfile 实机失败」根因很可能即此**。同代码库 `TsfInputProcessor.cs` 的 COM 释放为正确的单次 ReleaseComObject 范式，可对照修复（删除对 obj 的重复 Release）。

> **【2026-09-08 已修复】** `GetActiveTsKlid`/`ActivateTsProfile` 各删除对 `obj` 的第二次 Release，只对 `profiles`（同一 RCW）释放一次并加注释说明。TSF 活跃输入法识别与即时激活路径不再因 InvalidComObjectException 被 catch 吞掉。

**B7・Win11Layout 最近项双击触发（新发现，真实缺陷）**

`packages/shell/shell-start-menu/Windows/Layouts/Win11Layout.cs` 的 `CreateRecentChip`：row（Grid）与其父 hoverHost（Border）**同时**订阅 `MouseLeftButtonUp`（各执行一次 `ActivateOrLaunch`）与 `AttachNative`（右键菜单）。WPF 路由事件冒泡使单击一次两个 handler 均执行：第一次启动后进程尚未注册进 WindowTracker，第二次 `IsRunning` 仍为 false → **非单实例应用会被启动两个实例**；右键同理 `TryShowItems` 触发两次（菜单闪烁）。修复：仅在一个层级订阅或在 handler 内 `e.Handled = true`。

> **【2026-09-08 已修复】** 删除内层 row 的 MouseLeftButtonUp 与 AttachNative，只在外层 hoverHost 订阅一层（row 填满 hoverHost，点击必冒泡到外层），handler 内 `e.Handled = true`。单击只启动一次、右键菜单只弹一次。

**B8・四个状态面板窗口关闭后定时器 / 线程未清理（新发现，泄漏）**

`packages/shell/shell-menu-bar/Windows/` 下 `MemoryPanelWindow`、`MicrophonePanelWindow`、`SoundPanelWindow`、`NetworkPanelWindow` 四个面板的 ViewModel 持有 1s `DispatcherTimer`（SoundPanel 另有音频专用后台 worker 线程 `_audioWorkerRunning=true` 永不退出），窗口 `Closed` 均无 Stop/Dispose：计时器持有 refresh 闭包，闭包引用窗口控件树 → **面板每次开关都泄漏一整个控件树 + 每秒持续刷新已关闭窗口的 UI**（无效工作 + 内存累积）。同代码库已有正确范式可对齐：CpuPanelWindow 的 `CloseSelf`、SearchPopupWindow OnClosed 停止 debounce、PowerPopupWindow 的 IsVisibleChanged 启停、WifiPopupWindow OnClosed 停止置 null、ControlCenterWindow OnClosed 完整退订。

> **【2026-09-08 已修复】** 四面板统一为「按需刷新」范式（贴合用户按需加载原则）：各 ViewModel 增加 `Pause()`（停 1s DispatcherTimer）/`Resume()`（重启并立即刷一帧）/`CloseSelf()`（停 timer、释放 refresh 闭包；SoundPanel 额外置 `_audioWorkerRunning=false` 并 Pulse 唤醒音频 worker 退出）。窗口提升 `_vm` 字段，订阅 `IsVisibleChanged`（隐藏 Pause、显示 Resume）与 `Closed`（CloseSelf）。隐藏后不再每秒空转、不再经闭包钉住控件树；Sound worker 无任务时本就在 Monitor.Wait 休眠，停 timer 即不再投递。顺带清除 MemoryPanel 的硬编码占位初值（23.28/31.63 GB、0.736，改为中性「—」/0）。

**B9・BluetoothPopupWindow 诊断代码残留与泄漏（新发现）**

`packages/shell/shell-menu-bar/Windows/BluetoothPopupWindow.cs` 的 `StartScanning` 内：`_diagPulseTimer.Tick += ...` 每次打开重复订阅且永不 -=；`_uiWatchdog = new Timer(...)` 每次打开新建而旧实例不 Dispose（1s 后台 Timer 泄漏，回调闭包引用面板）；每次打开写盘 `bluetooth-diag.log`。注释自认「定位后移除」，但 StopScanning/OnClosed 均无清理 → 反复开关面板累积泄漏并持续写盘。收尾期应整体移除诊断基础设施。

> **【2026-09-08 已修复】** 整体移除诊断基础设施：删除 `Diag()` 写盘方法与 `_diagLock`、`_diagPulseTimer`、`_uiWatchdog`、`StartDiag()`、四个诊断计数器（`_lastUiPulse/_updatedEventCount/_bleEventCount/_renderCount`）及死字段 `_scanStart`，清除全部约 20 处 Diag 调用与仅服务于日志的 Stopwatch；StartScanning 不再调 StartDiag。ToggleDeviceAsync 的 catch 从「仅记日志」改为带说明的静默捕获（finally 全量枚举回真实状态）。StopScanning 本就完整停止/退订两个 watcher 与三个功能 timer，无需改动；扫描随 IsVisibleChanged 启停。不再产生 bluetooth-diag.log，也无 Timer/订阅泄漏。

**B10・NetworkPanelWindow 硬编码假数据（新发现，数据真实性）**

`packages/shell/shell-menu-bar/Windows/NetworkPanelWindow.cs` 的 `NetworkPanelViewModel` 八个字段初始值硬编码开发者本机快照（`58.241.3.98（查询中…）`、`中国 江苏 无锡市`、`10.2.83.51`、`282.8 KB`、`117.7 MB`、`26.5KB/s`、`277.4KB/s`、`163.2 MB`）。`NetworkCoreNative.IsAvailable=false` 或首次刷新前，面板**永久显示这些假数据**（公网 IP、地理位置、流量速率均非用户真实值），用户会误信为自己环境。MemoryPanelWindow 同理存在 `"23.28 GB/31.63 GB"` 占位（Attach 即覆盖，影响小，归 C 级）；NetworkPanel 的降级路径永久展示，归 B 级。修复：初值置空占位符（如 "—"），不可用时显示「不可用」而非示例数值。

> **【2026-09-08 已修复】** NetworkPanelViewModel 八个字段初值全部改为中性「—」，真实值由构造快照 / OnTick / 公网查询异步覆盖，原生统计不可用或离线时保持「—」，不再永久展示开发者本机快照。

**B11・CpuMenuBarExtension/ImeMenuBarExtension 跨线程更新 UI（新发现）**

`packages/shell/shell-menu-bar/Services/MenuBarExtensions.cs` 中两个扩展的 `OnChanged` 回调直接写 WPF 控件（`_label.Text` / `_iconImage.Source`），而 `IImeMonitor/ICpuMonitor.Changed` 由 StatusPoller 后台线程触发 → 跨线程访问 DispatcherObject 抛 `InvalidOperationException`，被 UpdateVisual 内 try/catch 吞掉 → **菜单栏 CPU/IME 图标在状态变化后永不刷新**（仅初始构建正常）。佐证：同代码库 `MenuBarStatusStrip.cs` 全部图标回调经 `UiDispatch.Run` 编队回 UI 线程（104 纪律注释），这两个扩展遗漏。修复：OnChanged 内用 UiDispatch.Run 包装控件更新。

> **【2026-09-08 已修复】** ImeMenuBarExtension / CpuMenuBarExtension 的 OnChanged 改用 `UiDispatch.Run(控件, ...)` 编队回 UI 线程（同线程直执行、跨线程 BeginInvoke），并对控件尚未构建（GetVisual 未调用）做判空。CPU 数值与 IME 图标在状态变化后可正常刷新。CalendarMenuBarExtension 用 DispatcherTimer（UI 线程 Tick）本就安全，无需改。

### C. 健壮性与卫生（低优先）

**C1・MenuCommandPipe 命名管道无 ACL / 鉴权**

`host/MenuCommandPipe.cs` 管道名固定 `BetterDesktop.MenuCmd`，默认安全描述符允许同用户任意进程连接并驱动 open-settings/dock-pin/toggle/convert/compress 等动作。同用户威胁模型下影响有限（可触发文件操作与设置翻转），但建议收窄管道 DACL 或加进程身份 / 一次性握手校验（P2 加固）。

> **【2026-09-08 已修复】** 同用户威胁模型下 DACL 无法阻止同用户进程，采用两层轻量加固：① 管道实例数限制为 1（单实例宿主）；② 协议 magic 握手（`BDMC1|` 前缀），服务端丢弃无 magic 的连接。文件头补充完整威胁模型说明与后续加固方向（进程身份校验 / 一次性 token）。

**C2・7z/rar 解压无路径穿越防护**

`ArchiveService.cs`：zip 走 .NET 内置防护（`ExtractToDirectory(overwriteFiles:false)` 内部校验）；但 7z/UnRAR/WinRAR 为裸 `-o<dir>` 解压，恶意压缩包条目可越界写。用户主动触发场景，风险中低，建议解压后校验产物路径或记录为已知限制。

> **【2026-09-08 已修复】** 解压前用引擎 list 命令（7z `l -ba` / UnRAR `lb`）枚举条目名，拒绝绝对路径或 `../` 越界条目（Zip Slip）；list 不可用时降级为正常解压（引擎自身报错）。rar / 7z / 7z→WinRAR 回退三条路径均已接线校验。

**C3・ArchiveService.RunProcess 无超时**

外部引擎（7z/Rar/UnRAR）`WaitForExit()` 无超时，引擎挂起时操作永不完成（Task.Run 下不阻塞 UI，但任务泄漏、无反馈）。soffice 引擎则有 120s 超时 + 杀进程树，可对齐。

> **【2026-09-08 已修复】** `RunProcess` 加默认 120s 超时，超时 `Kill(entireProcessTree: true)` 杀进程树并返回 -1，对齐 soffice 范式；新增 `RunProcessCapture`（捕获 stdout，供 C2 列表校验）同样带 60s 超时。

**C4・已跟踪二进制膨胀**

`git ls-files` 中二进制 37 个、合计 16.6MB：`tools/ShellComponentsPlayground/` 24 张截图（单张 0.5–1.3MB）、`shell-taskbar/Native/ExplorerTAP.dll` 等。截图类资产不建议入库，可移入本机目录或压缩后入库。

> **【2026-09-08 已修复】** `git rm --cached` 移除 Playground 全部 27 张 png 截图（保留本地文件），`.gitignore` 追加 `tools/ShellComponentsPlayground/*.png/*.jpg/*.jpeg` 规则；`ExplorerTAP.dll` 为运行时原生依赖，保留入库。跟踪二进制从 37 个降至 10 个（约 1.2MB）。

**C5・文档漂移残留**

docs/architecture.md「Effect 卸载时逆序**并行**执行」与代码（`PluginHandle.cs:162` foreach **顺序**执行）不符（旧报告 C7a 未修）；STATUS.md 阶段 / 测试数与实测不符；YamlDotNet 双版本并存。

> **【2026-09-08 已修复】** ① architecture.md 改为「逆序顺序执行（Enumerable.Reverse + foreach，非并行）」；② STATUS.md 全量更新（阶段→一阶段收尾定型、测试→15 工程约 375 用例、已实现范围→kernel 4 包 + shell 20 包 + 宿主、未实现范围→window-manager/theme/mission-control/spotlight/Companion）；③ YamlDotNet 统一为 16.0.0（Shell.Convert 从 15.1.6 升级，与 Kernel.Loader 对齐）。

**C6・空壳与 README 缺口（二轮核验修正）**

`packages/shell/shell-music/` 已含 `Services/KugouMusicApi.cs`（226 行，酷狗公开 API 实现，2026-09-04）与带 `## Known Limitations` 的 README（2026-09-07）——**并非空壳**，原文「仅一个契约（无实现、无 README、无 Known Limitations）」与事实不符（且与本节表格 shell-music=2 文件自相矛盾）。残余事实：该包整体未跟踪入库（git 中不存在，工作区新增）且无 UI 消费方（README 自述「当前无 UI 消费方，仅提供服务层」），收尾期应决定入库或归档。10 个测试工程缺 README、4 个包（shell-app-source/context-menu/convert/taskbar）README 缺 Known Limitations 小节（package-readme 红灯来源）两断言二轮核验成立。

> **【2026-09-08 已修复】** ① shell-music 已 `git add` 入库（4 文件）；② 10 个测试工程批量生成 README（测试范围 / 运行方式 / Known Limitations 三段式）；③ shell-context-menu / shell-convert / shell-taskbar 三个包追加 `## Known Limitations` 小节（shell-app-source 已有中文「已知限制」，保留）。

**C7・供应链漏洞（M11 实测，二轮核验扩大影响面）**

`dotnet list package --vulnerable --include-transitive` 二轮实测：**6 个项目**（Host、Shell.Convert、Shell.Convert.Tests、Shell.MenuBar、Shell.MenuBar.Tests、Tools.ShellComponentsPlayground）经依赖链解析到 **SixLabors.ImageSharp 1.0.0（2019 版）**，命中 7 条公告（含 3 条 High：GHSA-65x7-c272-7g7r 等）；slnx 其余 36 个项目无漏洞。根因单一：仅 `shell-convert` 直接引用 PdfSharpCore 1.3.2（csproj L26/L34），其依赖 SixLabors.ImageSharp 1.0.0 经项目引用**传递进宿主与菜单栏等生产路径**——原文「tools 工程非交付物、其余 49 个项目无漏洞」判断不成立，暴露面含生产交付物，修复优先级应上调（升级 PdfSharpCore/ImageSharp 或从依赖图剔除，随 B4 依赖治理一并处理）。

**修复状态（2026-09-08・C7 专项修复完成）**：`shell-convert` 已将 PdfSharpCore 1.3.2 替换为 **PDFsharp 6.2.4**（MIT，自带 PdfSharp.System 图像后端，无 ImageSharp 传递依赖），引擎代码同步适配（XUnit.FromPoint 消除 CS0618；加密强度改为库内默认 AES-256，移除 DocumentSecurityLevel 128 位赋值；Probe 报告 PDFsharp 6.2.4）。实测：全量构建 0 错 0 警；`dotnet list package --vulnerable` 42 项目全部归零；新增 PdfEnginesTests 6 用例（合并/合成/拆分/单页拒绝/加密往返/解密）全部通过，全量测试 374 通过 / 1 失败（A1 矩阵断言过期，与 C7 无关）；技术文档 markdown-conversion-hub.md 依赖段已同步回写。

**C8・StartMenuService.Show () 绕过 win-key 设置（新发现）**

`packages/shell/shell-start-menu/Services/StartMenuService.cs`：`Show()` 无条件 `_keyHook.Install()`，绕过 `Enable()` 中「startmenu.win-key=false 时不挂钩子（仅保留编程式 Show）」的意图 → 用户关闭 Win 键后按 Win 仍会弹菜单（设置语义被破坏）。修复：Show () 前检查 win-key 设置或改为安装仅一次的幂等逻辑。

> **【2026-09-08 已修复】** `Show()` 改为 `if (!_hookInstalled && _settings.Get("startmenu.win-key", true))` 才装钩子；用户关闭 Win 键后 Show 只做程序化弹出，不再偷偷装钩子，设置语义恢复。

**C9・StartMenuWindow 极小工作区异常（新发现）**

`packages/shell/shell-start-menu/Windows/StartMenuWindow.cs`：`ApplyLayoutSize` default 分支 `Math.Clamp(configuredWidth, 400, Math.Min(700, wa.Width))`—— 工作区宽度 <400 时 min>max，`Math.Clamp` 抛 `ArgumentException` 且未被捕获 → 菜单创建 / 布局切换失败；`PlaceInArea` 中 `height < MinHeight` 时强制 MinHeight 可能把窗口推出工作区。场景为极小屏 / 低分辨率虚拟机，概率低但一阶段定型期应记录。

> **【2026-09-08 已修复】** ① `ApplyLayoutSize` default 分支先算 `maxWidth = Min(700, wa.Width)`，下限取 `Min(400, maxWidth)`，极小屏时宽度收缩到工作区而非抛异常；② `PlaceInArea` 高度不足 MinHeight 时改为收缩到可用高度（`Math.Max(0, Min(MinHeight, 工作区高度))`），不再强制推出屏幕。

**C10・KeyboardLayoutInterop.AddLayout 插入位置（新发现）**

`AddLayout` 调用 `Reorder(existing, key, existing.Count - 1)`：key 不在列表时被插入到**倒数第二**（原最后一项保持最后），与「非默认则追加到末尾」的注释意图不符。修复：index 改为 `existing.Count`。

> **【2026-09-08 已修复】** index 改为 `existing.Count`（Reorder 内部钳制越界），非默认布局追加到末尾，与注释一致；顺带消除冗余三元 `setDefault ? 0 : (setDefault ? 0 : ...)`。

**C11・菜单栏通知按钮点击行为叠加（新发现）**

`MenuBarStatusStrip`：通知图标同时订阅 `border.MouseLeftButtonUp`（托盘折叠切换）与 CreateButton 的 `MouseLeftButtonUp`（打开控制中心）→ 单击同时触发托盘展开 / 收起与面板弹出两个动作（行为叠加，注释未说明；若属预期可忽略，但建议明确）。

> **【2026-09-08 已修复（方向二次校正）】** 设计确认：通知图标 = 通知中心 + 控制中心**同一图标空间双用**，按使用频率分左右键——**左键=控制中心（高频）、右键=通知中心（低频）**。修复：`OnButtonClicked` case Notification 按 `IsRightButton` 分发（左键 `ShowPopup` 控制中心；右键调 strip 新增的 `ToggleNotificationSwitch()`），`MenuBarStatusStrip` 中 `notifyToggle` 提升为字段并新增 internal 方法，移除原 border.MouseLeftButtonUp 的 Toggle 叠加 handler（消除与事件链的重复触发）；并删除从未装配的死类 `ControlCenterMenuBarExtension`（"◫"独立按钮，全仓零实例化，属历史残留）及 PlaygroundPreviewFactory 中的对应引用。**通知中心现状**：仅图标开关占位，尚未与系统通知接轨（无通知历史窗口/系统通知读取；已实现的系统通知能力只有 shell-notification 的"新装应用提醒" INotificationService，由 AppSourceChanged 自动触发，与菜单栏图标无关）——已在 NotificationToggle 类注释如实标注，待后续接入系统通知。

**C12・LiveTileCarousel 计时器不停止（新发现，二轮核验微调）**

`Win10Layout`：tile 注册条目清空后 DispatcherTimer 不 Stop（2.4s 空 tick 持续，近零开销但违背「仅在有条目时运行」的注释意图）；二轮核验：注释现文为「条目为空时 tick 直接跳过（近零开销）」，**「笔误」子断言不成立**（原文引文未复现），仅主断言「条目清空后不 Stop」成立。

> **【2026-09-08 已修复】** `Register` 条目为空时 `_timer.Stop()` 并 return，有条目才 Start；类注释同步改为「条目为空时停止计时器（不再空转）」。

**C13・StatusPlugin 事件驱动触发全量轮询（新发现）**

`shell-status`：音频 / 网络事件回调 `SetChangeCallback(_poller.PollNow)` 触发全量采集（1s 周期 monitor 被额外拉取）。幂等无害，但事件驱动应只刷新相关 monitor 或沿用防抖，资源可再省。

> **【2026-09-08 已修复】** 音频/网络事件回调改为 `OnStatusChanged`（150ms `System.Threading.Timer` 防抖），高频事件（音量滑块拖动、IP 续租抖动）合并为一次全量轮询；Unload 时 Dispose 防抖 Timer。与 StatusPoller 已有的前台切换 100ms / 电源 500ms cooldown 范式对齐。

**C14・测试空洞断言与计时等待（新发现）**

`kernel-hmr-tests/ExternalPluginAdapterTests`：`Assert.True(true)` 空洞断言 + `Task.Delay(500/5000)` 计时等待（第一轮精读发现）；`shell-context-menu-tests/GuidInfoTests`：两处 `Assert.True(true)` 空洞断言。此类用例在门禁统计中计为通过但实际无断言价值，建议补真实断言或标注为环境探针。

> **【2026-09-08 已修复】** ① ExternalPluginAdapterTests：测试 1 改用 `ping -n 6`（运行期不退出）+ 断言 `!IResourceSubject.IsQuarantined` + Unload 幂等；测试 2 改轮询等待 `IsQuarantined`（上限 8s，替代固定 5s）+ 断言 true。② GuidInfoTests：`GetIconLocation` 补可空元组结构断言（null 或 Path 非空 + Index≥0）；随机 GUID 用例标注「环境探针」DisplayName 并补类型断言。

**C15・RegistryVerbs 自研枚举引擎在原生菜单架构下整体闲置（三轮注释治理核实，原 ModernVerb 疑点扩大）**

> **【2026-09-08 已修复（用户拍板：删除）】** 用户复核后定论：最初自绘右键菜单时代该枚举引擎也并未发挥多大作用，且自绘桌面回归（2026-09-07 DesktopMenuPopup）后菜单项为手写基础项（打开/剪切/复制/删除… + ToolCatalog/转换/压缩挂点），注册表静态动词枚举仍无真实消费场景。执行删除：两个仍被 `ShellMenuInterop.EnumerateHandlers` 使用的纯辅助函数 PerceivedTypeOf/ResolveProgId/ProgIdValid **迁入 ShellMenuInterop.cs**（附「2026-09-08 迁移」注释），删除 `RegistryVerbs.cs` 主体 + `RegistryVerbTests.cs`、`RegistryVerbs20260903Tests.cs`（30 用例），同步更新 ContextMenuPlugin 预热注释与 ShellMenuInterop 白话索引。构建 0 错 0 警、345 用例全过。

## 四、旧审计报告（分析报告\_问题清单.md）12 项复核



| 原编号 | 问题                                          | 复核结论                                                                                                                                                                                                              |
| --- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | HmrManager 双锁不互斥                            | ⚠️ 实质缓解：所有字段写已收敛进 `lock(_gate)`，`_operationGate` 串行化操作；多字段一致窗口已极小                                                                                                                                                 |
| A2  | PluginHandle 重启竞态                           | ✅ 已修复：新增 `_restartGate` SemaphoreSlim 串行化重启（PluginHandle.cs:109-121）                                                                                                                                              |
| A3  | CordisContext.ActiveFiber 上下文级单字段           | ⚠️ 仍存：已改用 AsyncLocal（CordisContext.cs:20,36-41），并发加载归属仍依赖 HMR 串行化兜底                                                                                                                                               |
| B4  | CordisContext.\_plugins 僵尸句柄                | ✅ 已修复：`RemovePlugin` 同时清理 `_plugins`、`_injectCache`、治理器注册（CordisContext.cs:118-139）                                                                                                                               |
| B5  | ALC 回收经典陷阱                                  | ⚠️ 未变：仍依赖 GC 时机，未加弱引用 / 句柄释放校验（ADR-003 已登记为已知限制）                                                                                                                                                                  |
| C6  | ShellCorePlugin.Inject 漏声明 IVibrancyService | ✅ 已修复：`Inject` 现含 `IWindowHandleService` + `IVibrancyService`（ShellCorePlugin.cs:20）                                                                                                                              |
| C7  | 文档 / 契约漂移                                   | ⚠️ 部分修复：effect 逆序并行文档漂移仍在（C5）；LoadPluginAsync 已加载分支改为 `Unknown` + 显式消息；`IsAbiCompatibleWith` 现为 `Major== && CompareTo>=0` 全量比较（已修）                                                                                |
| D8  | DockWindow 接线半完成（object 占位）                 | ✅ 已复核（二轮核验修正：DockWindow.xaml.cs 现为 2889 行、dock 包 28 文件，原文「2601 行 / 24 文件」不成立）：构造注入完整（IVibrancyService/IAppearanceService/IDockAppsService/IDockIconService/DockLayoutService/DockVisualSettings/ISettingsService/IPlugin），无 object 占位接线 |
| D9  | AdvancedVibrancyService 空壳                  | ✅ 已复核：`shell-core/Vibrancy/AdvancedVibrancyService.cs` 已实现动态模糊与边缘光效（ACCENT\_ENABLE\_ACRYLICBLURBEHIND + alpha 打包），非空壳                                                                                             |
| D10 | WPF 线程风险                                    | ⚠️ 冒烟未崩，但全量精读发现 B11（Cpu/Ime 菜单扩展跨线程更新 UI）即属此类风险的实际落点                                                                                                                                                              |
| E11 | EventBus 默认值语义歧义                            | ✅ 已复核（EventBus.cs 全量 + EventBusTests）：空主题 EmitAsync 静默完成、handler 异常隔离、Waterfall 顺序折叠均有实现与测试锁定；原歧义点行为已明确                                                                                                           |
| E12 | AppSourceService.\_seen 并发                  | ✅ 已复核：`_seen` 读写全部收敛在 `_seenGate`（AppSourceService.cs:297,327），并发安全                                                                                                                                               |

## 五、本次审计发现的实现亮点（避免误伤）



1. 构建零警告零错误，369 用例覆盖良好，冒烟可启动。

2. 转换引擎工程质量高：soffice 用 `ArgumentList` 直传参数（禁 shell 拼串）、120s 超时 + `Kill(entireProcessTree)`；引擎按需下载带多镜像 + 大小 + SHA-256 校验，**校验和未配置即拒绝下载**（EngineDownloader.cs 红线落实到位）。

3. 右键菜单管理器安全网完整：注册表写前快照（RegTreeBackup）、非管理员走 HKCU 影子键等效禁用、保护项（open/opennewwindow 等）拦截确认、`HideBasedOnVelocityId` 特判。

4. StaComWorker 常驻 STA + `OleInitialize`，修复第三方 shell 扩展跨套间调用（有真机崩溃栈实证的工程记录）。

5. 新增两道门禁（verify-native-convergence、verify-no-cross-assembly-event）均带 Pester 单测并登记进注册表；cross-asm-event 确认 7 项已知违规均在基线内、无新增。

6. 决策记录格式 14 篇全合规、归档密封一致、md-links 145 个文件链接锚点全通、doc-budgets 26 篇在限内 —— 治理框架本身运转良好，问题在「执行与收尾」。

7. 全量精读确认的工程纪律亮点：COM 引用纪律普遍严谨（TsfInputProcessor、AudioInterop、ShellItemInterop、TaskbarAppearanceEngine 均有单次 Release /finally 释放范式）；原生结构体布局契约大多带红线注释与契约测试（NativeMethodsContractTests）；事件回调用途大量经 UiDispatch / Dispatcher 编队（104 纪律）；危险 P/Invoke（IUxUpdateManager vtable 序、wlanapi 变长结构、shell32 序数调用）均附 Open-Shell / SDK 对照注释。

## 六、修复优先级建议

**立即（半天内）**：① 同步更新 Pdf 源 Poppler 测试断言（或矩阵回退，二选一并定版）；② 恢复或重写被删的 T4T5 决策记录，为 9 月三大功能批次补齐 agent-note；③ 清理 overview.md 内部残留（内容归档、文件改名或删除）；④ 定版提交或回滚当前 WIP，使工作区回到「门禁全绿」基线。

**本周**：① 修 B3 治理器 Id 碰撞；② 修 B6 TSF 双重 Release（含验证 ActivateProfile 实机行为）；③ 修 B11 跨线程更新（UiDispatch 编队）；④ 修 B8/B9 面板与诊断代码泄漏（对齐 CpuPanel/SearchPopup 范式）；⑤ 修 B10 硬编码假数据（初值置占位）；⑥ GetCursorPos 收口到权威层并删宿主声明；⑦ 为 SplashWindow 走 ADR 追认或移入 shell 插件；⑧ 补 10 个测试工程 + 4 个包 README 缺口；⑨ 门禁扫描改为 `git ls-files` 清单（消除忽略目录造成的结构性红）。

**近期**：① B4 依赖治理（统一版本 + packages.lock.json + ADR 记录 ManagedShell 采用理由）；② C1 管道加固、C2 解压路径校验、C3 子进程超时；③ C4 截图移出 git；④ 真机演练 watchdog/recovery/reg-takeover 三条兜底链路；⑤ 按 C8–C14 与 B7 逐项小修（StartMenu 极小屏、AddLayout 位置、Win11 最近项双击、tile 计时器、事件驱动轮询粒度）。

## 七、审计局限与未覆盖项



* 全量精读为**静态代码审计**，未做真机交互验证（仅 smoke-test 4 秒）：B6 的 TSF 激活实机行为、B7 双开现象、B8/B9 泄漏的实测内存曲线、右键菜单 COM 加载、注册表夺权、watchdog 拉起、recovery 恢复，建议修复后真机演练另行取证。

* 反编译参考文件（tools/decomp 3 个 .decomp.cs）为第三方逆向转储，逐行确认其非产品源码，不参与产品质量结论；其余 502 个 .cs 均为产品 / 工具 / 测试源码并完成逐行精读。

* 供应链漏洞扫描（M11 建议的 `dotnet list package --vulnerable`）已在本报告生成时执行，结果以扫描输出为准。

* 本报告遵循仓库「一段一行」文档纪律撰写，不新增 md-wrap 债；但其自身为仓库外交付物，未纳入门禁范围。

## 八、独立复核核验记录（2026-09-08 晚间，另行会话二轮实测）

> 本节为对前文全部内容的独立二次核验：所有可测声明重跑实测命令，A/B 类代码级发现逐条源码锚定（文件:行），C 类全部抽查。核验期间未修改任何项目文件（本报告文件为唯一更新对象）。

### 8.1 实测命令复核（全部可复现，结果与原文比对）

| 项目 | 二轮实测结果 | 与原文 |
| --- | --- | --- |
| `dotnet build BetterDesktop.slnx` | ✅ 0 错误 / 0 警告；实际构建 44 个项目（slnx 42 + 依赖 2：shell-app-source / shell-desktop） | ⚠️ 0/0 一致；「50 个项目」修正为 44 |
| `dotnet test BetterDesktop.slnx` | 369 用例：368 通过 / 1 失败；失败项 `ConversionMatrixTests.Pdf源_Poppler目标登记`（Expected ["png","jpg","txt"] vs Actual 含 docx/xlsx，L70 断言过期） | ✅ 完全一致 |
| `pwsh scripts/run-gates.ps1` | 4 红 + 1 跳 + 9 绿：package-readme（10 测试工程缺 README + 4 包缺 Known Limitations）/ md-wrap（61 文件硬换行，含被忽略目录）/ architecture-guard（host\Views\SplashWindow.xaml）/ dotnet-format（15 个文件 IMPORTS IDE0005）；test-coverage 因依赖 dotnet-format 跳过 | ✅ 完全一致 |
| native-convergence | DllImport 89 / 上限 281 | ✅ |
| cross-asm-event | 已知违规 7 项基线内，无新增 | ✅ |
| smoke-test | 主程序启动，进程存活 4 秒未崩溃 | ✅ |
| agent-note / archived-notes / md-links / doc-budgets / gate-registry / host-log-sink | 全绿；md-links 检查 146 个文件（原文 145） | ✅（146 为微调） |
| `dotnet list package --vulnerable` | 6 项目命中 SixLabors.ImageSharp 1.0.0（7 公告含 3 High）；36 项目无漏洞；链：仅 shell-convert 直接引用 PdfSharpCore 1.3.2 → ImageSharp 1.0.0 → 传递进 Host/MenuBar | ⚠️ 影响面扩大，见 C7 修正 |
| Git 卫生 | 工作区 248 处变更（113M / 40D / 95?）；`packages/backups/` 被 .gitignore 忽略但门禁仍扫描计入（B5 成立） | ✅ |

### 8.2 代码级发现逐条核验结果

| 编号 | 核验结论 | 锚定证据（文件:行） |
| --- | --- | --- |
| A1 | ✅ 成立 | ConversionMatrix.cs:240-247（.pdf 已含 docx/xlsx PdfText）vs ConversionMatrixTests.cs:70-71（断言过期） |
| A2 | ✅ 成立 | `.agents/notes/2026-09-08-pinvoke-t4t5-decisions.md` git 已跟踪但工作区被删（git status D），archived 无归档；implemented 记录全部停留在 2026-08-19/20 |
| A3 | ✅ 成立 | overview.md（git 已跟踪）尾部含内部会话残留 `</think:6124c78e>` `<tool_calls:6124c78e>` 等 |
| B1 | ✅ 成立 | host/Bootstrap.cs 451 行；MenuService.cs / MenuCommandPipe.cs / ToggleKeyCommand.cs / DesktopToggleCommand.cs / FileLogSink.cs / Views/SplashWindow.xaml 齐全 |
| B2 | ✅ 成立 | host/Bootstrap.cs:449 DllImport GetCursorPos 与 shell-core/Native/NativeMethods.cs:116 重复 |
| B3 | ✅ 成立 | PluginHandleSubject.cs:24（Id=GetType().FullName 全插件同值）、ResourceGovernor.cs:63（按 Id 去重）、CordisContext.cs:137（同键注销） |
| B4 | ✅ 成立 | 0 个 packages.lock.json；YamlDotNet 16.0.0（kernel-loader）+ 15.1.6（shell-convert）双版本；ManagedShell 0.0.370 被 4 个包引用 |
| B5 | ✅ 成立 | `git check-ignore` 确认 packages/backups/ 被忽略，package-readme/md-wrap 仍将其计入违规 |
| B6 | ✅ 成立 | KeyboardLayoutInterop.cs:112-116（GetActiveTsKlid）与 132-136（ActivateTsProfile）：同一 RCW 双重 ReleaseComObject，异常被 catch 吞 |
| B7 | ✅ 成立 | Win11Layout.cs:179（row）+192（hoverHost）双订阅 MouseLeftButtonUp，186+199 双 AttachNative，无 e.Handled |
| B8 | ✅ 成立 | 四面板无 Closed/Unloaded 清理；SoundPanelWindow.cs:418 `_audioWorkerRunning=true` 从未置 false |
| B9 | ✅ 成立 | BluetoothPopupWindow.cs:94-108 StartDiag 每次打开重复 `Tick +=`、新建 _uiWatchdog 不 Dispose、写 bluetooth-diag.log；StopScanning/OnClosed 无清理 |
| B10 | ✅ 成立 | NetworkPanelWindow.cs:378-385 八字段硬编码本机快照（58.241.3.98 / 无锡 / 10.2.83.51 等） |
| B11 | ✅ 成立 | MenuBarExtensions.cs:113（_iconImage.Source）/196（_label.Text）直写控件，无 UiDispatch 编队 |
| C1–C5 | ✅ 成立 | MenuCommandPipe.cs:16/45 固定管道名无 DACL；ArchiveService.cs zip 受保护 / 7z-rar 裸 `-o`（L354/366/381）+ `WaitForExit()` 无超时；git ls-files 二进制 37 个 16.6MB；docs/architecture.md「逆序并行」vs PluginHandle.cs:162 foreach 顺序；STATUS.md 222/221 vs 实测 369/368 |
| C6 | ❌ 原文不成立 | shell-music 已有 KugouMusicApi.cs（226 行）+ README（含 Known Limitations）；整包未入库、无 UI 消费方（见 8.3） |
| C7 | ⚠️ 影响面扩大 | 见 8.1 漏洞扫描与 8.3 |
| C8 | ✅ 成立 | StartMenuService.cs:600-605 Show() 无条件 `_keyHook.Install()`，绕过 Enable() 的 win-key 设置 |
| C9 | ✅ 成立 | StartMenuWindow.cs:228 `Math.Clamp(configuredWidth, 400, Math.Min(700, wa.Width))` 极小工作区 min>max 抛异常 |
| C10 | ✅ 成立 | KeyboardLayoutInterop.cs:1098 `Reorder(existing, key, existing.Count - 1)` 插入倒数第二；顺带发现 `setDefault ? 0 : (setDefault ? 0 : …)` 冗余三元 |
| C11 | ✅ 成立 | MenuBarStatusStrip.cs:1050（notifyToggle.Toggle）+ 1194（CreateButton 内 ButtonClicked）同元素双订阅 |
| C12 | ⚠️ 主断言成立 / 子断言不成立 | Win10Layout.cs:1095-1102 Register 条目清空不 Stop；注释现文「条目为空时」无误（原文「条目不空时」引文未复现） |
| C13 | ✅ 成立 | StatusPlugin.cs:63/72 `SetChangeCallback(_poller.PollNow)` 事件驱动全量轮询 |
| C14 | ✅ 成立 | ExternalPluginAdapterTests.cs:27/44、GuidInfoTests.cs:64/74 共 4 处 `Assert.True(true)`；Task.Delay(500/5000)（L23/L39） |
| D8/D9/E11/E12（旧复核） | ✅ 成立 | DockWindow 构造注入完整；AdvancedVibrancyService.cs 109 行 ACCENT_ENABLE_ACRYLICBLURBEHIND + alpha；EventBus.cs EmitAsync/WaterfallAsync + EventBusTests.cs；AppSourceService.cs:297/306 `_seenGate` 收敛 |

### 8.3 事实修正汇总（相对原文）

| 位置 | 原文 | 二轮实测 | 处置 |
| --- | --- | --- | --- |
| 基线・项目数 | 50 个项目 | 44 构建成功（slnx 42 + 依赖 2；全仓 csproj 46） | 已改 |
| 分包计数 | app-source 13 / dock 24 / start-menu 24（表内自加 396） | 15 / 28 / 25（合计 403，与总分闭合） | 已改 |
| 覆盖口径 | 505 | 全仓非构建目录 .cs 506，排除 docs 草案 1 个即 505 | 补注 |
| C6 | shell-music 仅契约无实现无 README | 有 226 行实现 + README（Known Limitations），整包未入库、无 UI 消费方 | 已重写 |
| C7 | 仅 tools 1 项目，其余 49 无漏洞 | 6 项目命中（含 Host/Convert/MenuBar 生产路径），36 无漏洞 | 已重写 |
| D8 | DockWindow.xaml.cs 2601 行 / dock 24 文件 | 2889 行 / 28 文件 | 已改 |
| C12 子断言 | 注释「条目不空时」笔误 | 现文「条目为空时」无误 | 已撤 |
| md-links | 145 个文件 | 146 个文件 | 微调 |

### 8.4 核验局限

* 与原文相同的静态口径：B6 TSF 实机激活、B7 双开现象、B8/B9 泄漏内存曲线、右键菜单 COM 加载、watchdog/recovery 兜底等未做真机取证（原文第七节口径不变）。
* 「505/505 逐行精读」为原文工作成果；二轮核验仅锚定全部 A/B 类与全部 C 类点位（C 类为 100% 抽查覆盖），未重读全部 505 文件。
* 漏洞扫描（M11）已在本节实测执行，结果以扫描输出为准（6 项目命中）。

## 附录 A：可独立成公有板块的候选清单（全量精读维度）

> 前提：仓库已有 
>
> `docs/plans/archive/2026-09-07-common-capability-extraction.md`
>
> （P0-1 Native 收口 / P0-2 弹窗体系 / P0-3 面板注册机制，含 D1–D14 deferred）——
>
> **原为「仅规划未实施」**
>
> ；本次复核修正：**P0-1/P0-2/P0-3 在审计定稿时（2026-09-08）就已存在于工作区**（定稿时工作区 248 处变更 ≈ 当前 249 处，M113/D40 完全一致），定稿报告仅依据计划文档正文误判为「未实施」，本次按工作区实际代码修正。D1–D14 仍全部未实施。本附录基于 505 文件逐行精读，分三类：① 已立项（审计后部分落地，现状确认）；② 
>
> **全量精读新增候选（该计划未点名）**
>
> ；③ 已公共化（无需动作）。

### A1. 已立项（审计后复核现状）



| 板块                                         | 计划出处  | 现状（全量精读确认）                                                                                                                                                                                                                   |
| ------------------------------------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Win32 Native 收口（NativeMethods 平台层）         | P0-1  | **已实施**：`shell-core/Native/NativeMethods.cs` 432 行权威层 + `WinEventPump.cs`(167)/`MouseHook.cs`(54)/`MessagePump.cs`(27)；shell-taskbar/WinEventHook.cs、shell-window-tracker/WinEventHook.cs 删除；TaskbarAppearanceEngine/WindowTrackerService 改走 WinEventPump 单泵多订阅；WinEventForegroundPump 注释「仅保留泵的业务语义」 |
| 弹窗定位服务（PopupPositioningService）            | P0-2  | **已实施**：`shell-core/Windows/PopupPositioningService.cs`(82)；`PopupPositioningServiceTests` 新增；四处独立几何待逐处迁移确认                                                                                                                   |
| 弹窗基类上提（PopupWindowBase）                    | P0-2  | **已实施**：`shell-core/Windows/PopupWindowBase.cs`(196) 落地；MenuBarPopupWindow 迁移范围见附录 B2 复核                                                                                                                                                                       |
| 面板注册机制（IMenuBarExtension 公共化）              | P0-3  | **已实施**：`IMenuBarExtension`/`IMenuBarExtensionRegistry` 公共化至 shell-core/Contracts；`MenuBarExtensionRegistry.cs`(43) 实现；`MenuBarExtensionRegistryTests` 新增                                                                                                                          |
| MonitorBase / 监控注册表                        | D3    | 未实施：8 个 Monitor 各自实现轮询 / 防抖 / 事件（Ime/Mic/Volume 500ms、Cpu/Memory/Network 1s、Battery 3s、Brightness 事件驱动）                                                                                                                      |
| SettingsSectionBase                        | D2    | 未实施：7 个设置页 Section 的 TitleBlock 等构建方法重复（21/73 组）                                                                                                                                                                             |
| NullVibrancy 统一                            | D1    | 未实施：复核确认 4 处（MenuBarPlugin.cs:175 / DesktopPlugin.cs:643 / QuickNotePlugin.cs/ PlaygroundPreviewFactory.cs:203）                                                                                                              |
| ConversionEngineBase / StartMenuLayoutBase | D4/D5 | 未实施：convert 引擎已拆 17 个独立引擎文件（IConversionEngine 契约已立），样板基类未建；start-menu 4 布局 LoadIconAsync 重复仍在 |

### A2. 全量精读新增候选（计划未点名，建议收尾期立项）



| #  | 板块                                            | 证据（文件 / 行）                                                                                                                                        | 独立理由与收益                                                                                                                                                        |
| -- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N1 | **公共 AppBar 协商服务（AppBarService）**             | `shell-dock/Native/DockAppBarReservation.cs`、`shell-menu-bar/Native/AppBarReservation.cs`                                                         | 两套完整 AppBar 协商状态机（ABM\_NEW/QUERYPOS/SETPOS/REMOVE + 防循环 + 负高防护）各自实现；P/Invoke 声明虽已收口 shell-core，协商逻辑未合并。合并后 Dock/MenuBar 共享多屏生命周期，消除双实例注册冲突面                    |
| N2 | **公共面板 ViewModel 生命周期基类（PanelViewModelBase）** | MemoryPanelWindow / MicrophonePanelWindow / SoundPanelWindow / NetworkPanelWindow（B8 缺陷根因）                                                        | 四处重复「ViewModel + 1s DispatcherTimer + 无关闭清理」，CpuPanel/SearchPopup/PowerPopup 各写一套正确范式。基类统一：Timer 托管、Closed 自动 Stop、Dispose 幂等、后台 worker 生命周期 —— 修复 B8 并让新面板零样板 |
| N3 | **公共 UI 线程编队助手（UiDispatch 上提 shell-core）**    | `shell-menu-bar/Status/MenuBarStatusStrip.cs:2036`（internal）；各面板散落 Dispatcher.BeginInvoke                                                         | 正确范式存在但跨包不可用；B11（Cpu/Ime 扩展跨线程更新）即缺编队的实证。上提后统一回调编队 + 可单测                                                                                                       |
| N4 | **公共 COM 引用作用域（ComScope/ComRef）**             | `shell-status/Native/AudioInterop.cs:220`（私有 ComRef）、`poc/CoreAudioInterop.cs`（同款）                                                                | COM 释放配对纪律（7437）多处分叉实现，B6（KeyboardLayoutInterop 双重 Release）即同类治理对象。独立 using 作用域助手统一配对语义                                                                        |
| N5 | **公共输入法服务（ImeService，采集 + 管理合并）**             | `shell-status/Native/KeyboardLayoutInterop.cs` + `ImeMonitor`（采集侧）、`shell-menu-bar/Services/ImeLayoutEnumerator.cs`（管理侧：枚举 / 激活 / 增删 / 排序 / 中英切换） | 两套 KLID 枚举 / 映射 / 激活逻辑重叠（MenuBarStatusStrip.cs:755,818 / ImePopupWindow.cs 12 处调用点）；合并为 shell-core 输入法域（TSF/IMM 双路径），B6 修复一并落地                                 |
| N6 | **公共主题令牌绑定助手（ThemeTokenBinder）**              | BindTheme/SetThemeBinding 100+ 调用点遍布 menu-bar 各弹窗与 shell-desktop/FolderBrowserWindow（WifiPopupWindow 28 处、SearchPopupWindow、PowerPopupWindow 等）   | SetResourceReference 包装与令牌字符串各窗口重复；独立静态助手 + 令牌常量表（区别于计划 B4 的插件 SDK 外扩，本项是包内收口）                                                                                 |
| N7 | **公共注册表安全访问助手（RegistryGuard：快照→写→回滚）**        | `shell-context-menu` RegTreeBackup（唯一完整实现）；shell-status AutoStartManager、menu-bar ImeLayoutEnumerator 等注册表读写无快照纪律                                 | 右键菜单的写前快照是安全网范式；收口为公共助手后，所有注册表写路径默认带回滚能力（与 M 域「注册表夺权」兜底呼应）                                                                                                     |
| N8 | **弹窗定位服务（同 A1 P0-2，按全量精读补充依据）**               | PopupAnchor.Compute/ StartMenuPopup.BelowOf/ DockMenuPopup.AtCursor/ DesktopMenuPopup.Show 四处独立几何                                                 | 计划已立项；精读确认四处均缺 ABM\_GETTASKBARPOS 双路径与翻转（601 纪律），合并后统一补齐                                                                                                       |

### A3. 已公共化（无需新建，确认即可）

shell-core/Native/NativeMethods（wlanapi/SHAppBarMessage 等已收口）、AppLauncher（跨包启动器，shell-start-menu 引用）、VibrancyService/AdvancedVibrancyService、ShellEvents 常量类、MenuBarMetrics/MenuBarScreen（menu-bar 内公共几何件）、DockVisualSettings（dock 键集中管理）。

> 实施建议：N1–N5 与 A1 各立项独立收尾（与 B6/B8/B11 修复联动）；N6/N7 为低风险包内收口，可随门禁全绿批次一并处理；优先级不高于第一节 A 类与第三节 B 类缺陷修复。

## 附录 B：以「白话导航 / 逻辑线 / 按需加载」三原则为标尺的定稿验收维度

> 来源：用户 2026-09-08 拍板补充——「我想要的是有清晰全面的注释，任何 AI 接受项目时都可以根据我的白话定位到具体而准确的功能位置。任何功能都有清晰而通畅的逻辑线，没有交叉和缠绕，所有功能按需加载，追求流畅性。」本节据此重审 249 项工作区变更（2026-09-08 定稿后），并给出收尾验收口径。

### B1. 三原则定义与验收口径

| 原则 | 定义（用户口径） | 验收口径（可执行） |
| --- | --- | --- |
| 白话导航 | 任何 AI 接手时，凭一句白话需求（例：「把开始菜单最近使用改成 8 个」「把右键菜单的压缩项挪走」）能定位到具体而准确的功能位置 | 每个功能域有「白话 → 位置」索引；文件头注释写明「做什么 + 入口白话」；不存在只能靠翻找的空白带 |
| 逻辑线 | 任何功能从触发到完成是一条通畅的线，不交叉、不缠绕 | 单功能单路径；无 A↔B 回调缠绕；事件/回调线完整（跨线程有编队）；公共能力不反向依赖业务包 |
| 按需加载 | 功能用到才加载、不用即释放，追求流畅性 | 无全量常驻轮询/计时器；面板/弹窗关闭即释放；缓存有上限与过期；注册表/COM/系统 IO 不进热路径 |

### B2. 工作区现状复核（与定稿报告差异对照）

> 时间线修正：定稿报告基线即记录「工作区 248 处变更（113M / 40D / 95?）」。本次复核确认当前 249 处（113M / 40D / 96?）——**M/D 分类完全一致，定稿时工作区已是下述状态，报告未覆盖或误判；定稿后实际新增仅 1 个未跟踪文件**。下表为「工作区现状（= 定稿时工作区） vs 定稿报告表述」的差异修正，并非审计后新增功能。

复核口径：`git status` 249 项（153 文件 diff，+5061/-8780，大量删除 = 收口重构）；重新执行 build/test 实测。

| 实测项 | 结果 |
| --- | --- |
| `dotnet build BetterDesktop.slnx` | ✅ 0 错误 / 0 警告（44 项目），与定稿基线一致 |
| `dotnet test BetterDesktop.slnx` | ✅ 375 通过 / 0 失败（2026-09-08 C 类 + A1 修复后全绿；A1 断言已对齐真实矩阵） |

**工作区已定型板块（定稿报告未覆盖或误判）：**

| 板块 | 证据 | 三原则符合度 |
| --- | --- | --- |
| P0-1 Native 收口 | `shell-core/Native` NativeMethods 432 行 + WinEventPump(167)/MouseHook(54)/MessagePump(27)；shell-taskbar/WinEventHook.cs、shell-window-tracker/WinEventHook.cs 删除；TaskbarAppearanceEngine/WindowTrackerService 改走 WinEventPump 单泵多订阅 | 逻辑线 ✅（双实现合并单泵）；按需加载 ✅（单泵多订阅，7435 变体 B 反例消除） |
| P0-2 弹窗体系 | `shell-core/Windows` PopupWindowBase(196) + PopupPositioningService(82)；PopupPositioningServiceTests 新增 | 逻辑线 ✅（弹窗公共基类上提） |
| P0-3 面板注册 | `IMenuBarExtension`/`IMenuBarExtensionRegistry` 公共化至 shell-core/Contracts；MenuBarExtensionRegistry(43) + 测试 | 逻辑线 ✅（契约上提，MenuBarLeftZone 硬编码可注册化） |
| **右键菜单方向切换（2026-09-05/09-06 定型，定稿报告未覆盖）** | 自绘管线退役（MenuService/MenuHost/MenuSurface/ShellMenuContributor/UserMenuContributor/IMenuService 等删除）→ 渲染 = NativeMenuPopup（IContextMenu HMENU → TrackPopupMenuEx，explorer 同款；COM 全在 StaComWorker STA 线程）；管理 = MenuManagerService 系列（对标 ContextMenuManager 菜单项管理器：Create/Groups/Helpers/Style/Toggle + GuidInfo/ObjectPath/RegTreeBackup/ShellExMenuPreview/StaComWorker）；StartMenuWindow Deactivated 豁免、Win11Layout MenuSurface.Attach 移除；桌面图标右键 = DesktopMenuDelegation（自绘识别对象→系统菜单，跨进程 WM_CONTEXTMENU 委托退役）；桌面空白 = explorer DefView 转发；**dock 图标/应用抓取器 = shell-dock 自管例外（DockItemTemplate + DockMenuPopup）**；ContextMenuPopupWindow.cs 无消费方（退役残留未删） | **最符合三原则的现状**：自绘 MenuSurface 双路径与豁免关闭逻辑消失，逻辑线大幅简化；白话导航 ✅（MenuManagerService 头注释已写「启停/删除在 MenuManagerToggle.cs，新建/注入在 MenuManagerCreate.cs」） |
| 事件总线收口 | StartMenuService 设置订阅改 `IEventBus ShellEvents.SettingsChanged`；`IMenuService.Menus` 属性随自绘管线删除；Dispose 释放订阅句柄 | 逻辑线 ✅（跨程序集事件收敛） |
| 新包拆分 | shell-calendar（Contracts/Data/Services + 23 测试）、shell-music（Contracts/Services）；CalendarMenuBarExtension 注入 ICalendarService | 逻辑线 ✅（插件边界清晰，按需装配） |
| 转换引擎重构 | ConvertToPdfContributor/DocumentConversionService 退役 → ConversionService/ConversionMatrix/EngineRegistry/Engines（17 引擎文件 + IConversionEngine 契约）+ convert-tests 7 文件 | 逻辑线 ✅（引擎域按契约拆分） |
| WlanCore 原生层重编译 | wlan_core.cpp 122 行改动，DLL 18944→19968 字节 | — |
| 性能批次（已提交） | H1 MenuIconCache LRU 512 封顶 / H2 DockTickPolicy 双档降频 / H3 倒影 RTB 测量决策 + DPD 泄漏修复 / Wave-0 止血（Debug.WriteLine→DiagnosticLog.Trace、AccentBrush 推送契约回归、ImeMonitor 注入缝） | 按需加载 ✅（缓存有上限、tick 按档降频） |

**未修复缺陷（审计原文仍适用，锚点不变）：**

| 编号 | 缺陷 | 审计后复核 |
| --- | --- | --- |
| A1 | ConversionMatrixTests.Pdf源_Poppler目标登记 失败 | ✅ **已修复 2026-09-08**：断言更新为真实矩阵 [png,jpg,txt,docx,xlsx]，前 3 项 Poppler / 后 2 项 PdfText |
| B6 | KeyboardLayoutInterop 双重 ReleaseComObject | ✅ **已修复 2026-09-08**：两方法各删第二次 Release，同一 RCW 只释放一次（构建 0 错、Status 23 测试全过） |
| B7 | Win11Layout 双层 MouseLeftButtonUp | ✅ **已修复 2026-09-08**：删内层 row 订阅，只留外层 hoverHost 并 e.Handled，单击/右键只触发一次 |
| B8 | 四面板 timer 泄漏 | ✅ **已修复 2026-09-08**：四 ViewModel 增 Pause/Resume/CloseSelf，IsVisibleChanged 隐藏停/显示续、Closed 释放；Sound worker 可退出（MenuBar 27 测试全过） |
| B9 | BluetoothPopupWindow 诊断残留 | ✅ **已修复 2026-09-08**：Diag/双 timer/计数器/写盘日志整体移除，StopScanning 功能清理本就完整 |
| B10 | NetworkPanelWindow 硬编码假数据 | ✅ **已修复 2026-09-08**：八字段初值改中性「—」，Memory 占位初值一并清 |
| B11 | Cpu/Ime 扩展跨线程写 UI | ✅ **已修复 2026-09-08**：两扩展 OnChanged 经 UiDispatch.Run 编队回 UI 线程并判空 |
| C8 | StartMenuService.Show() 绕过 win-key 设置 | ✅ **已修复 2026-09-08**：Show() 仅在 win-key 开启时装钩子，关闭后只做程序化弹出 |
| C9 | StartMenuWindow Math.Clamp 极小屏异常 | ✅ **已修复 2026-09-08**：下限钳 Min(400,maxWidth)；PlaceInArea 高度不足收缩到可用区，不强制推出屏幕 |
| C12 | LiveTileCarousel 计时器 | ✅ **已修复 2026-09-08**：Register 空条目时 Stop，有条目才 Start；注释同步 |
| C13 | StatusPlugin 事件触发全量轮询 | ✅ **已修复 2026-09-08**：音频/网络事件回调改 150ms Timer 防抖，高频事件合并为一次全量轮询 |
| C11 | 通知按钮双动作叠加 | ✅ **已修复 2026-09-08（二次校正）**：通知图标双用——左键=控制中心（高频）、右键=通知中心（低频），case 按左右键分发；删除未装配死类 ControlCenterMenuBarExtension；通知中心未接系统通知已如实标注 |

### B3. 三原则下仍未达标项（差距清单）

| 原则 | 未达标项 |
| --- | --- |
| 白话导航 | **两级已全覆盖**：域入口层（19 shell 域 + host/Bootstrap + kernel/CordisContext）+ 文件内方法级白话索引（34 个核心/超大/缺陷文件，top25 大文件全覆盖） |
| 逻辑线 | B7/B11/C11/C13/A1 均已修复；剩余低优先 C 类（C1 管道高技能对抗、C2 旧引擎降级路径）不影响主线 |
| 按需加载 | B8/C12 已修复；剩余 Bootstrap 全量装配未评估（低优先） |
| 计划遗留 | D1–D14 全部未实施（附录 A1 已更新状态） |

### B4. 三原则收尾落实建议

1. 白话导航：**两级已落地**——域入口层（19 个 shell 域 Plugin + host/Bootstrap.cs + kernel/CordisContext.cs，详见 B5）+ 文件内方法级白话索引（34 个核心/超大/缺陷文件，top25 大文件全覆盖，详见 B6 治理记录）。
2. 逻辑线：B7（事件双订阅）、B11（跨线程断链）已于 2026-09-08 修复；剩余 C11/C13 为低优先收尾项。B11 的根治性上提仍可与附录 A2 N3（UiDispatch 上提 shell-core）联动。
3. 按需加载：B8 已于 2026-09-08 以「IsVisibleChanged 隐藏 Pause/显示 Resume + Closed CloseSelf」范式修复（等价达到 N2 目标，PanelViewModelBase 基类抽取仍可作为后续去重项）；剩余 C12（carousel 空转）为低优先。
4. 定稿验收门：三原则逐项核验通过 = 收尾完成；B6–B11 已全部核销，剩余未达标项为 A1 与 C 类（见「未修复缺陷」表），验收前不得宣称定型。

### B5. 开源前注释专业化治理记录（2026-09-08 二轮，面向开源可读性）

本轮以「开源后任何读者/AI 凭白话定位、注释不指向不存在的东西」为标准做了一轮全仓注释治理，构建保持 0 错 0 警。

**死代码删除**

- `shell-context-menu/Windows/ContextMenuPopupWindow.cs`：自绘菜单管线退役后的无消费方残留（全仓仅自身引用），已删除。

**白话导航块（域入口全覆盖，共 21 处）**

- shell 19 域：app-source、calendar、context-menu、convert、core、core/animation、desktop、dock、menu-bar、notification、pinning、quick-note、recent、search、settings、start-menu、status、taskbar、window-tracker 的 `*Plugin.cs` 统一在 namespace 后加【白话导航】块（白话需求 → 精确文件/方法）。
- 宿主层 `host/Bootstrap.cs`（启动顺序/命令行/看门狗/日志/图标恢复导航）；内核 `kernel/Core/CordisContext.cs`（插件契约/容器/HMR/加载器/资源治理导航）。

**过时/幽灵注释修正（注释指向已退役或不存在的类型，开源最易被看笑话处）**

| 文件 | 问题 | 修正 |
| --- | --- | --- |
| DesktopWindow.cs（2 处） | 注释称右键「统一经 IMenuService」，该接口已随自绘管线退役 | 改为「图标 → NativeMenuPopup；空白 → DesktopMenuDelegation 转发 explorer DefView」 |
| DockWindow.xaml.cs | `_showFallbackStartContextMenu` 注释称「呈现统一走 IMenuService 弹层」 | 实际走 DockMenuPopup，已改 |
| RegistryVerbs.cs | 「见 ShellMenuContributor」指向已删类 | 改为现存 ShellMenuInterop |
| RegistryVerbs.cs（2 处） | 「见 ExplorerCommandInterop」指向**从未存在的类** | 如实标注：ModernVerb 已产出、IExplorerCommand 消费链接入 deferred（仅测试覆盖）——同时是一条新发现的功能缺口 |
| ToolCatalog.cs | 「消费方 UserMenuContributor + ContextMenuSection」两者均已删 | 改为真实消费方 DesktopIconsControl |
| MenuManagerSection.cs | 「对齐 ContextMenuSection 先例」（该类已并入自身） | 改为「分区自包含，不依赖外部样式工厂」 |
| DockPlugin.cs | 注释称「AppGrabber 已废弃，由开始菜单接管」，但代码仍订阅 shell.appgrabber.show 唤起它（自相矛盾） | 改为「管理 Dock 本体与应用提取器」 |
| 6 文件 8 处 | Launchpad 独立窗口已融合进 AppGrabber，注释仍把它当现存组件并列（DockPlugin×2、AppSourcePlugin、IAppIconService、Win32ShellIconService、VibrancyMapping、AppearanceService、BackgroundDiffusionTests） | 统一改为「应用提取器」；ShellWindow.cs 说明融合关系的沿革注释保留 |
| DesktopIconsControl.cs | 「旧自绘路径见 Build*Menu」指向已删旧方法名 | 改为现存 BuildIconMenuEntries/BuildBackgroundMenuEntries + NativeMenuPopup 渲染 |
| RegistryVerbs/ShellMenuInterop（3 处） | 「垃圾文本/垃圾指针」口语 | 中性化为「未解析原文/无效指针」 |
| ContextMenuPlugin.cs | 注释括号不配对 | 修正 |

> 注：表中 RegistryVerbs.cs 三行（L497/498/504）是注释治理期间的历史记录；该文件已随 C15 决策删除（2026-09-08），此处保留备查。

**编码核实（排除一处虚惊）**

- 全仓 .cs 经严格 UTF-8 字节校验：0 个非 UTF-8 文件（BOM/无 BOM 均合法）；此前 PowerShell 控制台所见「乱码」仅是控制台代码页显示问题，GitHub 上不会乱码。DesktopPlugin.cs 为 UTF-8 BOM + 混合行尾（641 CRLF/11 LF），已用保持 BOM/主流行尾的方式插入，未改变其余行尾。

**保留未动的注释类型（属专业实践，非遗留问题）**

- 沿革注释（「替代 X / 从 X 搬迁 / X 已退役」）：解释设计变迁，保留；
- StartKeyHook 的 v4–v6「已废弃」版本演进记录：说明当前方案为何成立，保留；
- 真 TODO 仅 1 处（SystemSection.cs:255 启动延迟逻辑，已写清接入方式）；
- docs/TERMINOLOGY.md、ADR-002、design-proposals 等文档引用均核实存在。

### B6. 方法级白话导航下沉（2026-09-08 三轮）

域入口块解决「白话 → 文件」后，本轮把导航下沉到「文件内白话 → 方法」：在 34 个核心/超大/缺陷文件的 namespace 后加「本文件方法级白话索引」块（白话 → 方法名，用 ── 分隔样式区别于域级块），构建保持 0 错 0 警。**top25 大文件已 100% 覆盖。**

- **右键菜单域 7 个**：NativeMenuPopup、DesktopMenuDelegation、MenuManagerService/Toggle/Create（partial 三件套互链）、ShellMenuInterop、MenuManagerSection（RegistryVerbs 已于 2026-09-08 删除，两个辅助函数迁入 ShellMenuInterop，见 C15）。
- **B6–B11 缺陷文件 9 个**（修复前先让定位零成本，索引中直接标注缺陷行号）：KeyboardLayoutInterop（B6/C10）、Win11Layout（B7）、MenuBarExtensions + MenuBarStatusStrip（B11）、Memory/Microphone/Sound/Network 四面板 + BluetoothPopup（B8/B9/B10，索引点明面板族同构关系）。
- **超大/高频/内核文件 17 个**：DockWindow.xaml.cs（2601）、DesktopIconsControl（2097）、MenuBarStatusStrip（2072）、AppGrabberWindow（1386）、KeyboardLayoutInterop（1192）、BluetoothPopup（1155）、WifiEnumerator（980）、Win10Layout（952）、AppSourceService（878）、ControlCenterWindow（820）、AppearanceService（796）、Win7Layout（756）、ImePopupWindow（704）、DesktopBrowser（665）、DesktopWindow（632）、WifiPopupWindow（607）、StartMenuService（595）、SearchPopupWindow（590）、DesktopPlugin（域入口 + 方法级双层）、HmrManager（内核热重载）等。
- 方法：提取方法签名 → 按白话功能分组 → Python 插入器保持各文件 BOM 与主流行尾（CRLF/LF 混合文件不被破坏）；*Plugin.cs 采用「域入口块在前、方法级索引在后」双层结构。
- 后续：新增大文件或重构拆分时按同一范式补方法级索引；中小型文件靠类/方法 XML 注释与域入口块即可，不做运动式铺量。

### B7. C15 处置结论（2026-09-08 三轮 → 最终删除）

见第三节 C15：RegistryVerbs 经用户拍板**已删除**（初判「暂不删、文件头加当前地位标注」→ 用户复核定论：自绘菜单时代该枚举作用有限、当前无消费场景），两个仍被 ShellMenuInterop 使用的辅助函数 PerceivedTypeOf/ResolveProgId/ProgIdValid 迁入 ShellMenuInterop.cs，RegistryVerbs.cs 主体与 RegistryVerbTests.cs、RegistryVerbs20260903Tests.cs（30 用例）一并移除，相关注释同步；构建 0 错 0 警、345 用例全过。


### C16. 信息源 API 独立化：BetterDesktop.Api 契约包（2026-09-08 四轮，全部落地）

**背景（用户野心）**：「未来会依托我们软件制作很多的外部扩展（对标 deepseek-harness），
需要把所有的信息源提供 API 全部单独拎出来，给其他创作者提供空间。」
本举措 = 附录 A「可独立成公有板块」候选的一次性系统性落地。

**形态决策（用户拍板：按推荐 + 一次全搬）**

- 新建独立契约包 `packages/api/BetterDesktop.Api`（**零项目依赖**，仅 BCL + WPF 基础类型），
  把 68 个契约文件（约 40 个 public 接口 + 模型 + 事件）从 16 个 shell-* 实现包迁入；
- **保留文件内命名空间不变**（`BetterDesktop.Shell.Status.Contracts` 等原样）→ 全仓 `using` 零改动，
  各实现包只把 ProjectReference 指向 API 包（18 个包改引用）；
- **shell-plugin-sdk 并入**：其 `IShellPluginWindow`/`PluginWindowConfig`/`ThemeSnapshot`/`SkinKind`
  （插件窗口契约）迁入 API 包后，原包删除（slnx 同步更新）；
- 接口与实现同文件的做**拆分**（契约入 API 包、实现类留原包）：`IWeatherDayProvider`+`NullWeatherDayProvider`、
  `IShellExMenuPreview`+`MenuPreviewItem` 两处；
- 采集内部测试缝 `ISystemSource`（依赖 Native 类型）、internal 接口（`IStartMenuLayoutHost` 等）**不搬**，留在实现包。

**关键发现与修复**

1. **接口依赖实现类（交叉缠绕）**：`IStartMenuLayoutProvider.BuildLayout(StartMenuService)` /
   `IStartMenuSectionProvider.BuildSection(StartMenuService)` 直接引用实现类 → 违背「无交叉缠绕」。
   抽新契约 **`IStartMenuDataService`**（布局数据面：ThemeTokens/GetAllApps/GetProgramTree/
   GetPinnedStartMenuApps/GetSectionProviders/GetRecentPrograms/GetUserName/OpenSettings/
   RefreshLayout/GetNewlyInstalledApps/GetIconAsync/ShowLayout/GetMenuStyle/ActivateOrLaunch/Hide/
   Settings/GetRecentCount），StartMenuService 实现之，布局/栏目提供者只依赖数据契约。
2. **XML 注释治理（开源专业度）**：API 包开启文档生成后暴露原包隐藏问题——record 参数上非法注释
   （WindowInfo/RunningAppInfo/MenuRequest）、cref 引用实现类/枚举成员未限定（IAppearanceService/
   IHolidayProvider/IAppSourceService），全部修复为合法 `<param>` 与全限定 cref。
3. **命名空间遮蔽根除**：API 包带入 `BetterDesktop.Shell.Convert` / `BetterDesktop.Shell.Dock`
   根命名空间后，命名空间链优先于 using 别名，导致 `Convert.ToByte`（System.Convert）与
   `Dock.Left`（WPF 枚举）在引用 api 的包里解析失败。全仓 18 个引 api 包一次性改为
   **全限定** `System.Convert.ToXxx` / `System.Windows.Controls.Dock.X`（13 个文件 25+ 处），
   规范写入 docs/extension-dev.md §7 供未来代码遵循。

**验证**：`dotnet build` 0 错 0 警；`dotnet test` **345 用例全过**（与迁移前基线一致）。

**产出物**

- `packages/api/BetterDesktop.Api.csproj`（零依赖契约包；slnx 新增 /packages/api/ 项目）；
- `packages/api/<域>/`（Status / AppSource / Calendar / Notification / Search / WindowTracker /
  Recent / Taskbar / Music / Dock / Settings / StartMenu / Desktop / ContextMenu / Convert / Core /
  PluginWindow 十七个域目录）；
- `docs/extension-dev.md`《外部扩展开发指南》：契约总览 + 生命周期 + 信息源消费/扩展点实现示例 +
  注册顺序 + 三原则 + 命名空间遮蔽规范 + 构建门禁；
- 已删除：`packages/shell/shell-plugin-sdk/`（内容并入 API 包）。

**重复/复用审计（迁移中顺带）**

- `ISystemSource` 保留为采集层测试缝（单测注入 Fake），与对外信息源面不重复；
- `NullWeatherDayProvider`（空实现）作为契约默认值随接口入 API 包，业务实现 `WeatherProvider` 留实现包——
  分工清晰无重复；
- 未发现同名同语义接口跨包重复；`Convert`/`Dock` 遮蔽为唯一全局性命名冲突（已根除并写入规范）。

