bin/
obj/
.vs/
*.user
*.suo
*.binlog
artifacts/
publish/
**/TestResults/
.DS_Store
Thumbs.db

# 本机构建产物（非 bin/obj 命名）
host/Debug/
host/Release/

# 旧代码快照目录（本地保留，不入库）
backups/

# 内部自动化状态
.workbuddy/

# 反编译/本地工具缓存与二进制
tools/ilspy/

# 历史遗留垃圾构建目录
**/obj2*/

# 构建日志与探索 POC（不入库；**/Native/build/ 为 CMake 产物目录，现有 bin/obj 规则不覆盖）
b4.txt
poc/
**/Native/build/

# 本机运行日志与临时脚本
Temp/

# C4: Playground 开发参考截图不入库（体积大且为个人环境快照）
tools/ShellComponentsPlayground/*.png
tools/ShellComponentsPlayground/*.jpg
tools/ShellComponentsPlayground/*.jpeg

# 一次性会话修复脚本（保留但不入库）
scripts/_scratch/

# Rust 工程构建产物（Cargo target/，engine / engine-index / native/convert-engine）
**/target/

# 发布产物目录
dist/

# 本机门禁/构建日志（历史遗留，不入库）
gates.log
gates-run*.log
build-tmp.log

# 旧代码快照/备份目录（任意层级，本地保留不入库）
**/backups/

# 第三方转换引擎完整安装包（calibre / tesseract / ffmpeg / libreoffice，体积大 + 第三方二进制与许可证）
#
# 【必须锚定根目录：开头的 / 不能省】
# 写成 `engines/` 时，gitignore 的**无斜杠模式在任意深度匹配** —— 于是
# `packages/shell/shell-convert/Services/Engines/` 也被吞掉（Windows 下 git 的
# 匹配对大小写不敏感，`Engines` 撞上小写规则）。后果实测：该目录里 3 个**新**文件
# （LiteEngine.cs / LiteCapability.cs / ComPdfTwoHopEngine.cs）从未入库，
# 而它们在磁盘上存在、本地能构建 ⇒ `git status` 干净、本地构建绿、干净检出却编译失败。
# 这是 B1「干净检出构建不出来」的真凶：最阴的一种，因为所有本地信号都显示"正常"。
# 锚定成 `/engines/` 后只匹配仓库根的那一份（已核对：根 engines/ 仍被忽略，不再有源文件被吞）。
/engines/
