# 右键菜单工程功能审查报告（shell-context-menu）

> 审查日期：2026-09-03
> 审查对象：`packages/shell/shell-context-menu/`（32 个 C# 源文件，5594 行，不含 bin/obj）
> 审查方式：skill 静态审查脚本（code_reviewer.py）+ 全量源码人工通读
> 结论速览：**架构与工程质量整体优秀；发现 3 个 P1 功能缺陷、4 个 P2 问题、若干 P3 事项；脚本报出的 15 个"严重问题"经逐条核实全部为 C# 误报。**

---

## 一、脚本审查结果（code_reviewer.py）

| 指标 | 数值 |
|---|---|
| 扫描文件 | 36（含 obj 生成文件） |
| 严重问题 | 15（**全部判定为误报**，见下） |
| 一般问题 | 0 |
| 优化建议 | 45（全部为"代码行过长 >80 字符"） |
| 注释覆盖率 | 12.53% |
| 华为 Java 规范评分 | 未参与（C# 不评分） |

### 脚本"严重问题"逐条核实 —— 全部误报

| 脚本报出 | 实际内容 | 判定 |
|---|---|---|
| 14 × "硬编码敏感信息"（BuiltInOpsContributor.cs / MenuService.cs / MenuIconCache.cs / ShellMenuContributor.cs 等） | 命中的是 `IconKey = "sys:E894"`、`OpacityKey`、`FlattenKey` 等 **"Key" 词素正则误报**，无任何密钥/凭据 | 误报 |
| 1 × "字符串拼接构建 SQL"（ShellMenuInterop.cs:241） | 该行是 `DiagnosticLog.Trace` 里的 `string.Join(" | ", …)` 诊断日志拼接，**工程内无任何数据库访问** | 误报 |

45 条"优化"均为 >80 字符行——本项目 C# 行宽惯例为 100+（`FileClassifier.cs:80` 187 字符确属偏长，可顺手断行），属风格基线差异，不构成实质问题。注释覆盖率按 Java 独立注释行统计，低估了本工程大量高价值的**行尾踩坑注释**（实测通读体验：注释密度与质量远高于 12.5% 的表观值）。

---

## 二、架构总评

**分层清晰、纪律一致，是本仓库成熟度最高的工程之一。**

- **分层**：`Contracts/`（纯数据模型，零依赖）→ `Services/`（组装管线 + Win32/COM 互操作）→ `Windows/`（独立弹层窗口承载）→ `Sections/`（设置分区 UI），依赖方向单向。
- **管线固化**：`MenuService.BuildItems` 严格按「模板 → 贡献项（异常隔离）→ 五区块排序（MenuGroup 顺序显式声明防漂移）→ 能力过滤（隐藏优先，递归）→ Shift 扩展过滤 → Opening 注入点（订阅者异常隔离）」执行；`TrimEdges` 兜底悬空分隔线，防御完整。
- **秒开纪律**：RegistryVerbs / COM handler 均「缓存命中即出 + 未命中后台预热返回空」；图标 SHGetFileInfo 后台提取、菜单先出后补——Win11 式"右键转圈"根因系统性规避（唯一违例见 P1-2）。
- **实测踩坑知识固化**：MonitorFromPoint 的 POINT 按值传参签名陷阱、FrameworkElementFactory.SetValue 禁忌、弹层定位"四边缘检测"语义标注"禁止回退"、STA 编组、ShowActivated 自愈——这类注释是可复用的工程资产。
- **M10 错误静默纪律**：全工程 try/catch + `DiagnosticLog.Trace` 覆盖一致，单贡献者/单 handler/单项注册表解析失败均不拖垮整体。

---

## 三、发现的问题

### P1（建议尽快修复）

#### P1-1 `MenuAccessKeys.FindExplicitMarker` 数组越界

```csharp
// MenuAccessKeys.cs:72-84
for (var i = 0; i + 2 < text.Length; i++)
{
    if (text[i] != '(') continue;
    if (text[i + 1] is '_' or '&')
    {
        var ch = text[i + 2];
        if (text[i + 3] == ')')   // ← 文本以 "(_X" 结尾时 i+3 == Length，越界
```

循环条件只保证 `i + 2 < Length`，`text[i + 3]` 在文本形如 `"压缩(_S"`（缺右括号）时抛 `IndexOutOfRangeException`。触发源可控：注册表 MUIVerb、COM 菜单文本、用户自定义工具名都可能含此形态。该调用位于渲染路径 `MenuHost.Fill`（经 `MenuService.ShowAsync`）——经 `MenuSurface.Attach` 接入时被外层 try/catch 吞掉表现为"右键无菜单"，经 `IMenuService.ShowAsync` 其他调用方则可能未处理异常。
**修复**：循环条件改 `i + 3 < text.Length`（并把 `ch` 提取下移到括号校验之后）。

#### P1-2 `ToolCatalog.Detect` 在 UI 线程同步跑全表扫描（违反本项目秒开纪律）

```csharp
// UserMenuContributor.cs:96  ← Build 在 UI 线程（MenuService.BuildItems）同步调用
foreach (var d in ToolCatalog.Detect())
// ToolCatalog.cs:25-84 ← 冷缓存时：15 组候选 File.Exists + HKLM/HKCU App Paths 全表枚举
```

`RegistryVerbs` / `ShellMenuInterop` 都严格执行「注册表扫描永不进 Build 同步段」，唯独 `ToolCatalog.Detect`（60s TTL，冷启动后首次右键或过期后必 miss）在 Build 同步段做注册表全枚举 + 数十次 `File.Exists` 磁盘探测——**首次右键存在可感卡顿**，与本工程自述的 §5-2 秒开纪律直接矛盾。
**修复**：给 ToolCatalog 加与 RegistryVerbs 同款的「miss 返回空 + Task.Run 后台预热」模式；或进程启动时预热。

#### P1-3 两个进程级缓存无界增长（RegistryVerbs.Cache / ShellMenuContributor.ComCache）

```csharp
// RegistryVerbs.cs:41
private static readonly ConcurrentDictionary<string, (List<RegistryVerb> Items, DateTime Stamp)> Cache = new();
// ShellMenuContributor.cs:30 —— 键 = "com|kind|path1;path2;..."
private static readonly ConcurrentDictionary<string, (List<ShellVerbItem> Items, DateTime Stamp)> ComCache = new();
```

缓存键包含**完整目标路径**，每个不同文件一条；TTL 只在命中时校验，过期条目**永不清理**——右键浏览过大量不同文件后只增不减（且 verb 项含 Children 树、COM 项含 Invoke 闭包，单条不小）。对比：`MenuIconCache` 已按 H1 治理为 512 容量 LRU，这两个同量级缓存漏治。
**修复**：套用 MenuIconCache 同款容量封顶 LRU（或定期清扫过期条目）。

### P2（安排修复）

#### P2-1 `RegistryVerbs.Invoke` 用 `cmd.exe /c` 执行注册表命令串

```csharp
// RegistryVerbs.cs:178-184
FileName = "cmd.exe",
Arguments = "/c " + command,
```

路径含 `&`、`^`、`%` 等 cmd 元字符时会被 shell 解释（如 `C:\a&b\file.txt` 被截断成两条命令——既是功能 bug 也是命令注入面）；`BuildCommand` 把 `%1` 替换为带引号路径，若原命令本身已含引号则引号嵌套在 cmd 下解析歧义。explorer 的语义是拆分 exe + 参数后走 CreateProcess。
**建议**：短平快缓解——对替换后的路径参数再做 cmd 元字符转义；根治——按首段拆 exe、参数走 `ProcessStartInfo.Arguments` 时不经 cmd（与记忆中ContextMenuManager 实证语义对齐）。

#### P2-2 `ShellMenuInterop` 的 pDataObj COM 泄漏

```csharp
// ShellMenuInterop.cs:197-201  SHCreateDataObject 产出 pDataObj
// ShellMenuInterop.cs:277  "_ = pDataObj; // Marshal 释放交给终结器（RCW）"
```

`pDataObj` 全程只是 `IntPtr`，**没有任何托管 RCW 包装，因此没有终结器，引用计数永不释放**——每次 COM Query 泄漏一次 DataObject 分配（频率受 60s 缓存抑制但持续累积）。`comObjects` 中的 handler RCW 靠 GC 终结释放同理偏弱。
**修复**：`SHCreateDataObject` 出参改 `Marshal.GetObjectForIUnknown` 包装 RCW（或 finally 里显式 `Marshal.Release(pDataObj)`，注意 Invoke 闭包若需持有 DataObject 需相应延长生命周期——当前闭包只持 contextMenu，pDataObj 可安全释放）。

#### P2-3 设置页功能状态 roadmap 腐烂（对用户展示错误状态）

```csharp
// ContextMenuSection.cs:450-454 —— 标 🔧 的 4 项实际均已实现：
//   "Shell 扩展 verb 透传"      → ShellMenuContributor + ShellMenuInterop 已落地
//   "菜单项图标渲染"            → MenuHost.AttachIcon（sys:/file:/tool: 三协议）已落地
//   "回收站「还原」；解压到…"   → RecycleRestore + ZipOps 已落地
//   "快捷工具按文件类型过滤"    → UserMenuContributor.AutoToolMatchesTarget 已落地
```

设置页是用户可见的功能台账，与代码实态不符会误导用户与后续维护者。
**修复**：4 项改 ✅，或把清单改为从单一来源（如 MENU-SPECS.md）生成。

#### P2-4 `ShellNewCatalog._cache` 非线程安全发布

```csharp
// ShellNewCatalog.cs:22  private static List<ShellNewEntry>? _cache;
// ContextMenuPlugin.cs:67 _ = Task.Run(ShellNewCatalog.Enumerate);  ← 后台预热
```

后台预热线程与 UI 线程首次访问存在竞态：可能双跑全表枚举（HKCR 全子键扫描，代价不小），且 `List` 引用发布无 volatile，可见性无保证（当前 .NET x86/x64 内存模型下实际良性，但属契约级隐患）。
**修复**：改 `Lazy<T>` 或 `Interlocked.CompareExchange` 发布。

### P3（低风险 / 顺手修）

| # | 位置 | 问题 |
|---|---|---|
| P3-1 | ContextMenuPopupWindow.cs:71 | 超高菜单 `MaxHeight` 用 `SystemParameters.WorkArea`（**主屏**），混合 DPI 副屏更矮时封顶不准；有 VerifyOnScreen 平移兜底，但平移可能把窗口推出顶边而非内滚。建议所在屏工作区高度（GetWorkAreaPhysical 已有，除以 DPI 转 DIP） |
| P3-2 | MenuItemDef.cs:12 vs RegistryVerbs.cs:113 | Text 长度契约不一致：注释写 ≤60，`IsTextValid` 用 <80 |
| P3-3 | ShellNewCatalog.cs:133-137 | ShellNew 的 Command 分支不验证目标程序是否真的创建了文件，直接返回预期路径（菜单可能立刻刷新出不存在的项） |
| P3-4 | UserMenuContributor.cs:106 | 自动工具 Id 用 `string.GetHashCode()`——.NET 字符串哈希进程内随机化，Id 跨进程不稳定（当前无跨进程消费方，无害但脆弱；建议改路径规范化哈希如 SHA1 截短） |
| P3-5 | IMenuService.cs / MenuService.cs:126 | 低层入口 `ShowAsync(items, pos)` 不做 TrimEdges/扩展过滤，契约未注明"调用方需自保证无首尾悬空分隔线" |
| P3-6 | ContextMenuSection.cs:385-393 | 工具作用域下拉仅 2 项（DesktopIcon/Desktop），`UserMenuSpec.Scope` 其余 6 个枚举值只能手编 settings.json |

---

## 四、验证过的关键正确性（亮点清单）

以下此前踩坑修复点均在当前代码中确认到位、无回归：

1. **ContextMenuPopupWindow.DefaultShowActivated => true**（45 行）——弹层激活自愈豁免在位。
2. **MonitorFromPoint 已用 `NativePoint` 结构体按值传参**（313-321 行）——工作区检测失效根因已根治。
3. **MenuService 会话管理全部 Interlocked**（Dismiss/DismissInternal/NotifyClosedOnCompletion），Closed 事件在完成时清 `_active`，`IsOpen` 不会永久卡 true。
4. **MenuIconCache**：容量封顶 LRU + 并发重复提取 raced 兜底 + 负缓存可逐出 + Freeze。
5. **MenuHost 图标后台补齐**：未命中走 `Task.Run` + `Dispatcher.BeginInvoke(Background)`，`item.IsLoaded` 防关窗后回填。
6. **命令执行顺序**："先关窗、后经 Dispatcher(Input) 执行命令"（MenuHost.Fill 303-323），内联重命名的焦点依赖契约成立。
7. **Esc 分层收起**（HasOpenSubmenu 递归判定）、右键落自身窗口即关（自愈 1）、800ms 未激活自关（自愈 2）、出屏物理域像素差平移兜底。
8. **MenuText.FromWin32 / MenuAccessKeys 协作**：`&X`/`&&`/`(&X)` 三形态解码正确；同层助记唯一（P1-1 的越界除外）。
9. **FileClassifier**：回收站项不给 Open（防"打开必然失败"假项）、快捷方式目标失效降级 Unknown、多选能力取交集——能力过滤判定链完整。
10. **`context-menu.migrated` 回退开关**经 grep 确认在 shell-desktop 侧有真实消费方（DesktopIconsControl.cs:98），设置页开关非死键。

---

## 五、结论与修复优先级建议

| 优先级 | 项 | 预估工作量 |
|---|---|---|
| 立即 | P1-1 越界（一行循环条件 + 位置下移） | 10 分钟 |
| 立即 | P1-3 两缓存 LRU 化（直接复用 MenuIconCache 模式） | 1-2 小时 |
| 本周 | P1-2 ToolCatalog 后台预热 | 1 小时 |
| 本周 | P2-3 roadmap 状态对齐（纯文本） | 10 分钟 |
| 排期 | P2-1 cmd 元字符转义 / P2-2 pDataObj 释放 / P2-4 Lazy 发布 | 各 0.5-1 小时 |
| 顺手 | P3 各项 | — |

脚本（针对 Java 设计的正则规则）在 C# 工程上严重问题误报率 100%，其统计数值仅可作参考；本报告以人工全量通读结论为准。
